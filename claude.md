# trading Claude Instructions

## 語言規範
- **所有回覆請使用繁體中文**，包含說明、建議、程式碼注釋（非英文關鍵字）。

## 專案概覽
- `trading` 是回測系統：FastAPI 後端（`backend/`）+ 靜態前端（`frontend/`）+ 資料模組 + 策略引擎。
- 部署於 Railway，推送 `main` branch 自動觸發部署（GitHub repo: `joanzzgo-jpg/trading-backtest`）。
- Railway 用 `Procfile`／`railway.toml` 直接跑 `cd backend && uvicorn main:app`，**沒有跑 `start.sh`**。前端 JS 打包由 `backend/main.py` 的 `_build_js_bundle()` 在 **import 時**自動執行（偵測來源 JS 比 bundle 新就重建）→ Railway 部署開機時自動重建，不需 `start.sh`。
- ⚠ **本機**改前端 JS 後：執行中的 uvicorn **不會**重建 bundle（`--reload` 只看 .py）→ 重啟服務，或 `cd backend && python3 -c "import main"` 手動重建（執行中的服務每次請求直接讀磁碟 bundle＋即時算 `?v=` mtime 版號，重建完重新整理即可，不必重啟）。

### 快速啟動
```bash
cd /Users/noah/trading && ./start.sh
# 或直接進後端：
cd /Users/noah/trading/backend && uvicorn main:app --reload
```

### 推送前冒煙測試（守門員）
```bash
node scripts/smoke_e2e.js   # 需本機服務跑著；於裝過 puppeteer-core 的目錄執行
```
進場→K棒/標記→真拖曳平移(驗範圍有變)→縮放→切4H→零JS錯誤。**改前端/圖表相關程式後、推送前必跑**。⚠ headless 進場唯一正解=`window._landingEnter()`（點城門按鈕會被登入鎖擋、互動全打在城門頁上＝假測試）。⚠ 寫拖曳測試要先讀 `#mainChart` 的 rect 把幅度夾在圖內（主圖實際只有 ~1150px，用 ±500 當起點會落到圖外＝空操作，曾據此得出錯誤結論）。

### 動到 K 線倉庫後（守門員之二）
```bash
cd backend && python scripts/repair_klines5m.py     # 只掃描；出現「新」破洞才回傳碼 1
```
⚠ 2026-07-31 起：現存 15 個洞全是**永久性**的（2018-19 幣安中斷、XAUT 永續上市日之前）→ 舊版「有洞就回 1」等於**永遠回 1**、當守門員是狼來了。改成只有不在腳本 `_KNOWN_HOLES` 白名單裡的洞才算失敗（`--strict` 可看原始全貌）。新增白名單項目前，先跑 `--fix` 確認它真的補不回來。

**倉庫也會「落後」，那是另一回事**（2026-08-18 加 `--extend`）：`--fix` 只補**已存區間內**的洞，
補不了「倉庫停在 8/12、今天是 8/18」。落後的代價實測 **9.6 倍**：同一段 5m，倉庫涵蓋 8.0ms、
落到 API 77.0ms。定期跑：
```bash
cd backend && ../.venv312/bin/python scripts/repair_klines5m.py --extend   # 補到今天
../.venv312/bin/python scripts/repair_klines5m.py                          # 再確認沒有新破洞
git add backend/data/klines5m/   # ★ 是版控檔，要 commit 才會上 Railway
```
⚠ 5m 的「淨增」會顯示 **0**：`_KEEP_DAYS["5m"]=370`，尾巴長多少頭就被砍多少 →
**判斷有沒有補到要看尾端日期，不是看根數**。

### 動到即時更新／背景補載後（守門員之四）
```bash
node scripts/check_continuity.js     # 需本機服務跑著；約 14 分鐘（要等真實 K 棒收盤，別縮短）
```
⚠ `/api/latest` 每次只回 **2 根** → 即時輪詢一中斷（分頁凍結／電腦休眠／斷線）中間那幾根就永遠不到，
**且完全不報錯**：使用者只看到「K 棒斷掉、要重整才好」。一般冒煙測試（載入→拖曳→切時框）
再跑幾次都碰不到，因為它從不「中斷再恢復」——這支專門製造中斷，是唯一抓得到這形狀的測試。
已驗證它**抓得到**（植回舊碼 → 4 分鐘/8 分鐘兩項失敗、回傳 1）。
⚠ 缺口補回靠 `_bgLoadNewerBars`，它只支援 `window._BG_TF` 那幾個時框；**1w/1M 補不動 →
必須退回「照接」**（寧可留洞也別讓圖表凍住）。`_scheduleGapFill()` 的回傳值就是在講這件事，別改成無回傳。

### K 棒不可以「自己動」（守門員之七）
```bash
node scripts/check_bar_stability.js            # 需本機服務跑著；預設 1m / 150 秒
```
盯 `ohlcvData` 逐欄比對，**判準一定要分級**（不分級會把正常行為誤判成 bug，我第一版就踩了）：
age=0 形成中（h/l/c 本來就會變＝正常，但 **open 永遠不該變**）／age=1 剛收盤補最終值＝正常／
**age≥2 早就定案的棒動了＝bug**／外加硬不變式 **low ≤ min(open,close) 且 high ≥ max(open,close)**
（違反＝那根是兩份快照縫出來的，沒有別的可能）。
⚠ 根因與修法（2026-08-08，使用者：「最新 K 棒還是會動」）：資料來源在 binance/bybit 之間反覆跳，
兩家對同一根已收盤 K 棒差 4~15 點。四道防線缺一不可 ——
①後端 `_sticky_source`（來源黏著 60 秒，**換源那一拍回空**、不可回舊快照：回舊的會讓新舊值輪流蓋，
實測同一根 ±9.9 來回跳）②`/api/latest` 的 `src` 存進快取跟著資料走（`last_fetch_source()` 是
**thread-local**，走快取時回的是別的請求的來源→假換源）③前端補正一律**整根換或整根不動**＋
**單調閘門**（權威值的範圍要涵蓋我們這根才補：同源的最終值必然涵蓋半路值，跨快照的替換必然被擋）
④`_srcRealign` 要**連續兩次**看到新來源才觸發整段重對齊。
實測 180 秒：定案棒改寫 38 → **0**、不可能 K 棒 → **0**、形成中 open 變動 → **0**。

### 新增/移除時框、或改任何資料源後（守門員之八）
```bash
cd backend && ../.venv312/bin/python scripts/check_tf_spacing.py
```
每個時框實際請求一次，驗**回來的 K 棒間隔中位數真的等於那個時框**（crypto/us/tw 共 16 個組合）。
專治本檔一直在講的那個坑：「對照表漏列 → `.get(tf, 預設)` 靜默退回日線」——圖上有東西、不報錯。
★ 這支**不看程式碼、不看對照表**，直接問「你回給我的資料間隔對不對」→ 不論哪張表漏列、
哪個 fallback 退化都抓得到。已驗證：把 `us_stock.TF_MAP` 的 1h 拿掉 → 立刻報
`us 1h: 間隔 86400s ≠ 3600s`。
⚠ 別改成「檢查對照表有沒有這個 key」那種啟發式：實測**會誤報**（Pionex 沒有 1m 是回空表讓上層
fallback、`YF_TF_MAP` 是盤中專用函式本來就不管日/週/月）。會叫狼來了的守門員比沒有更糟。

### 動到 crypto fallback 鏈後（守門員之五）
```bash
cd backend && ../.venv312/bin/python scripts/check_perp_not_spot.py
```
⚠ **永續（`.P`）絕不可降級成現貨**。2026-08-08 抓到：前端送 `exchange=binance`，`.P` 就走不進
`fetch_crypto_ohlcv` 的永續專用鏈，落到「無 .P」那段 → fapi 一失敗**第二步就是 `_fetch_binance`＝現貨**。
實測主圖 BTC/USDT.P 顯示 64980.26＝Binance **現貨** 64980.25，永續各家 64952~64953（**差 28 點／4.3bps**）。
使用者的回報是「主圖跟合約行情數值有時候對不上」——**完全不報錯**，圖上有 K 棒、時間也對，只是換了商品；
而且現貨棒混進永續序列會生出假 FVG／錯收盤／接縫，還會被倉庫固化。這支專測「主要來源掛掉時降級到哪」，
已驗證抓得到（植回舊條件 → 拿到的值距現貨 0.00）。⚠ 基差 <1 時它回 2＝**測試不成立**，不是通過。

### 切時框/標的後圖層殘留（守門員之六）
```bash
node scripts/check_tf_switch_layers.js     # 需本機服務跑著
```
⚠ `renderAll` 會用 `_lastFVG*` 把圖層重畫回來（修「切換後標記消失」用的），但切換當下那幾份裝的還是
**上一個時框**的資料。標記層看不出來（`_has()` 會過濾），但 FVG 逐筆止損/止盈**線**沒有這層過濾
（直接 `timeToCoordinate`）→ 大時框的進場時間在小時框上找得到座標，上百條紅綠虛線一次冒出來
（使用者：「切時框會出現很多線條」）。修法＝`loadData` 開頭 `_resetLayerCacheOnCtxChange()`，
**必須在 `fetchWinRate()` 之前**（勝率快取命中是同步填好那幾份的，清在後面會把正確資料一起洗掉）。
⚠ 2026-09-12 起 FVG 交易線預設隱藏且**不下載** → 腳本開頭必須先 `toggleFVGTrades(true)`，否則永遠回 2（測試不成立）—— 這支就這樣安靜失效了 5 天才發現。
⚠ 判準要看**內容指紋**不能只數條數：切完本來就會有線（本機快照 `_snapPaint` 秒畫的是**這個**時框的資料，
那是對的），只有畫成舊時框那一批才是 bug。一般冒煙測試抓不到——它只驗最終狀態，而這個 bug 活在
「切換瞬間～新勝率回來」那段窗口裡，最後會被正確資料蓋掉。

### 動到台股收盤補齊的「進度」後（守門員之二十二）
```bash
cd backend && ../.venv312/bin/python scripts/check_tw_fill_progress.py   # 不需服務跑著，約 10 秒
```
2026-09-21 使用者：「台股興櫃的價格在本機跟 railway 上不同，railway 上是對的」。
實測本機 **203 檔興櫃**與線上不同，打 cnyes 核對 **12/12 都是線上對、本機錯**（本機停在 9/18 的價）。
根因＝`_fill["seen"]` 記的是 **`_asked`（我打算問的那批）**，不是來源真的回答的那批 →
本機睡醒後 DNS 解不到 `mis.twse.com.tw`、cnyes 也連不上 → **整批抓失敗照樣被記成「補過了」**，
一輪就湊滿 2698 檔 → `done` 蓋下去當天不再補。日誌寫的是「收盤補齊完成：2698 檔／**1 輪**」，
看起來完全正常。★★ 這就是本檔一直在講的最毒形狀：**失敗被記成有效狀態**，畫面零跡象。
→ `fetch_tw_quotes_bulk(symbols, answered=set())` 回填「真的問出確定答案」的代號，
worker 改記 `_answered`；兩邊都掛掉時這輪進度＝0，下一輪自然重問，網路回來就自己補上。
⚠ 「確定的答案」有三種、都算問到了（否則長尾永遠補不完＝無限打上游）：
**回應裡有它**（有沒有成交都算）／**兩種前綴都問到了都沒有**／**剛確認過查無還在 `_TW_NOQ` 內**。
不算的是：那個 chunk 連線失敗（`_quote_pass` 會跳過）、被 `_TW_NOQ` 擋掉這輪根本沒送出去。
⚠ 用盡 60 輪仍沒補完時**不可以印「完成」**：改印 ⚠ 並講明「未補到的仍是前一個交易日的價」。
⚠ 回傳碼 2＝連不上 cnyes（測試不成立），不是通過。已植回舊行為證明會失敗（整批失敗記 10/10、回傳 1）。
★ 通則：**「問過了」與「問出答案了」是兩件事** —— 任何拿「我發出去的清單」當進度的地方，
上游一掛就會把失敗記成完成。進度一律記**對方真的回答的那批**。

### 動到股票資料來源後（守門員之三）
```bash
cd backend && ../.venv312/bin/python scripts/check_tw_sources.py
```
**台股為主，兼驗美股/港股**（同一個坑三個市場都中過，見下）。
⚠ 台股接連出過兩次「安靜壞掉」：①30m/2h 時框閘門漏列 → 拿到的其實是**日線**（圖上有東西、不報錯）；
②條件式抓取的 ETag 與解析結果分家 → 清單從 1972 檔掉到 **50 檔**，且來源檔案內容沒變會一直 304、
**自己不會好**。兩個都「跑一輪看起來完全正常」，要跨輪／跨呼叫端順序才現形 → 這支專測多輪＋交錯順序，
並驗各時框分桶根數。已驗證它**抓得到**該 bug（植回舊碼會失敗、回傳 1）。
⚠ 「時框對照表漏列 → `.get(tf, 預設)` 靜默退回日線」這個坑，**台股/美股/港股三個市場都中過**：
台股漏 30m/2h（`resample_tw` 退回 1d）、美股/港股漏 30m/2h（`TF_MAP.get(tf,"1d")` 退回日線）。
新增時框或改資料源時，先確認每個市場的對照表都有該時框、且沒有靜默 fallback。

⚠ **上游會「半壞」，不是只有全好或全掛**（2026-09-17 全套守門員掃出來的兩個既存問題，⑧⑨ 注入式驗證、結果確定）：
①上櫃全量 opendata（解壓約 3.9MB）連抓 12 次斷 2 次，全是 `ChunkedEncodingError`＝**內容下載到一半被關**。
`http_pool` 的 urllib3 Retry **管不到這段**（它只管到收到標頭）→ 那一輪約 900 檔上櫃股變成沒價格、約 120 檔不見
（公司名單也失敗時整批消失 2681→1689），碰不到「縮水一半」門檻＝零錯誤。修＝`_get_body_retry`＋失敗時沿用上一份解析結果。
②鉅亨 charting API 約 5~10% 回 **HTTP 200** 包著 `{"statusCode":5031,"message":"Redis連線錯誤"}` → 被當成沒資料 →
當日 K 退回落後約 20 分鐘的 yfinance。修＝`_cnyes_data` 看 statusCode≥500 重試 **1 次**。
★ 錯誤是**一陣一陣**的（實測 75 秒內 9 次全擠在同一段 4.3 秒）→ 別在請求路徑裡多重試/長等待，整段錯誤靠 `cnyes_last_good` 墊。
★ 「真的沒資料」是 statusCode 200＋`t` 空陣列 —— 不可重試。
③上櫃那份實測有一次**拖了 685 秒**才斷（另一次 36 秒）：requests 的 `timeout` 是「**兩次收到資料之間**」的上限，不是整個下載的上限 → 對方每隔幾秒吐一點就能無限拖（清單背景更新整個卡住、守門員曾卡 17 分鐘）。修＝`_get_body_retry` 串流讀＋`_TW_BODY_BUDGET`(30s) 計時器到點 `shutdown` socket（光 close 叫不醒卡在 recv 的讀取）。⑩ 用本機假伺服器每 0.2 秒吐 64 bytes 驗證（舊寫法 12 秒後仍卡著）。
★ 通則：**任何會下載大檔的背景工作，都要有「整體」時限，不能只靠 timeout 參數。**
④冷啟動那一輪（手上還沒有上一份可沿用）上櫃沒抓到 → 約 900 檔沒價格。`main.py` `_tw_worker_nap`：清單裡 >200 檔沒報價就 5→10→20 秒提早重抓、之後回 30 秒（正常沒成交的只有 30~50 檔）；日誌 `清單不完整…提早重抓`。
（盤點過：天氣全用 aiohttp `ClientTimeout(total=…)` 本來就有整體時限；加密走 Binance/Bybit CDN 無慢吐證據，最高頻路徑不動。）
⚠ ⑥⑦ 分桶判準改用**資料自己最後一根的時間**縮放（原本用「現在幾點」→ 來源一落後就叫狼來了）；落後只印提示。日線冒充盤中棒時最後一根在開盤前，照樣判失敗。② 的 304 改成直接看狀態碼（原本比耗時，那一輪還含 MIS 請求，網路一慢就誤報）。

### 上方列時框置中（`node scripts/check_topbar_rows.js`）
時框列「絕對置中」改由 `ui.js` **實際量**放不放得下才加 `.topbar.tf-centered`（原本 CSS 寫死 `min-width:1500px`，
依「右側按鈕 530px」推導；右側後來長到 616px → 1500~1600 寬時框壓在按鈕上，1536＝常見 1920×125% 筆電）。
★ 寫死的寬度門檻一定會隨按鈕增加而過期（同守門員之九的教訓）。量右側寬度要量**可見子元素的範圍**，不量容器（並排模式容器會被 flex 撐大）。
⚠ 守門員寬度清單原本只有 1440/1600，剛好漏掉最嚴重的 1536 —— 已補 1536/1680。

### 往 topbar 加任何元素後（守門員之九）
```bash
node scripts/check_topbar_reachable.js   # 需本機服務跑著；360/375/390/820/1200 五種寬度
```
`.topbar-right` 是一排**只會越加越多**的圖示按鈕。它自己不是捲動區時，排在最後的那幾顆會直接
落在視窗外 —— `.topbar` 不捲、`body` 又 `overflow-x:hidden` → **永遠點不到，畫面上零異常、零錯誤**。
2026-08-13 我加 `#netSig`+`#drawLayers`（+114px）就把「我的交易」「VWAP」推出去了；
⚠ 而且修正前只剩 **6px** 餘裕 → 360px 的 Android 早就中了 → 修法要對任何寬度成立，
**不是把新元素藏起來**（下一顆按鈕又會把它推出去）。判準＝每顆可見按鈕 `elementFromPoint` 命中。
⚠ ★★ 這支的判準**不可以用 `scrollIntoView()`**（我第一版就是，植回舊碼照樣通過＝叫不出狼）：
`overflow-x:hidden` 只擋使用者、不擋程式改 `scrollLeft`。只捲「overflow-x 是 auto/scroll 且真有溢出」
的祖先，量之前把 `documentElement.scrollLeft` 歸零。詳見 memory `project_topbar-right-overflow`。

### 電腦休眠/分頁凍結 → K 棒補不回來時要明講（2026-09-21）
`/api/latest` 每次只回 2 根，輪詢一中斷中間那幾根就永遠不到，而且**完全不報錯**。
`_scheduleGapFill()` 會自動補，但只支援 `window._BG_TF` 那幾個時框（1w/1M 補不動）——
**補不回來時原本沒有任何人告訴使用者**。→ `realtime.js` `_chartStaleTick/_chartStaleCheck`
＋主圖上方的 `#chartStale` 提示條（附「重新整理」鈕）。
・偵測「睡過」＝**計時器漂移**（每 5 秒一次，真實間隔超過 90 秒＝被凍結），與市場開收盤無關。
・⚠⚠ 只有在**真的沒修好**時才出聲：醒來後給 12 秒讓輪詢/補載追，再驗三條，缺一不報 ——
  ①剛剛真的睡過 ②最新一根 K 棒年齡 > 2.5 個時框 ③**價格正在動**（`window._lastPxMoveTs`，
  由 `_pushCurPrice` 在價格真的變動時寫）。沒有③的話，台股/美股收盤後或週末一睡醒就會誤報。
・⚠ 比對 K 棒時間要用**原始 ISO 時間**，`toTime()` 回的是圖表時間（+8 小時）。
・補回來就自動收掉（已顯示時每輪重驗）。

### 連線四格（`utils.js` `_quality`）的門檻是量出來的，不是抄 ping 的（2026-09-21）
⚠ **不可以套 ICMP ping 的分級**（<20ms 很好／100+ 差）：我們量的是**整個 HTTP 往返含伺服器處理**。
實測本服務下限：線上 `/api/tickers` 中位 **99ms**（香港邊緣→新加坡後端，純往返 84.5ms 已是地理極限）、
本機 ~20ms。套 ping 標準會讓線上所有人都是「差」；而舊門檻 <150ms 給四格又讓所有人恆為四格＝零資訊。
→ 依本服務實際區間：4 格 <120ms／3 格 <250ms（行動網路）／2 格 <600ms／1 格 其餘或心跳停／0 格 離線。
⚠ 兩個「宣稱自己不知道的事」已修：①沒有 RTT 樣本時原本回**滿格**→ 改 -1＝未知（空心格）
②樣本窗原本每 2 秒把同一個 `_tkRttMs` 再塞一次 → 輪詢一停整窗都是舊值、看起來還很健康
→ 改成同一次量測只收一次 ＋ 超過 20 秒沒新樣本視為未知。

### 自訂 DOM 標籤蓋在價格軸上時，底下那格刻度要讓位（2026-09-20）
現價／十字線價格標籤是**自訂 DOM**（LWC 原生標籤已關掉）→ LWC 不知道它們存在，底下的刻度照畫，
兩個數字疊在一起（實測橘色「80,343.6」上緣透出灰色「80000.0」）。使用者最早回報的
「右邊價格那行會重疊看不清」就是這個；**調標籤透明度只是遮，遮不掉半露在標籤外的那半截字**。
→ `charts.js` `_axisTickText` 掛在 `localization.priceFormatter`：落在標籤範圍內的刻度回空字串。
⚠ 不可以在 formatter 裡呼叫 `priceToCoordinate`（渲染中再進渲染）→ 改在更新標籤時先把
「要蓋掉的價格區間」算好（`_axisHideSet`），formatter 只做數字比較。
⚠ 沒被蓋到的刻度要**維持原本格式**：用 series 自己的 `priceFormat.precision` 走 `toFixed`。
加千分位的話整排刻度都會跟著變（BTC「80000.0」變「80,000.0」），一眼看得出來。
⚠ 標籤隱藏時要清掉抑制帶，否則刻度會永遠缺一格。
⚠ 成交量軸是 `visible:false`，所以這個 chart 層級的 formatter 只影響右側價格軸；副圖各有自己的設定。

### 動到靜態檔壓縮／預壓產物後（守門員之二十一）
```bash
cd backend && ../.venv312/bin/python scripts/check_static_br.py   # 需本機服務跑著；約 5 秒
```
2026-09-20：/static 的 .js/.css 開機時預壓成 `.br`（`main.py _precompress_br`，quality 11），
請求帶 `Accept-Encoding: br` 直接送那份 —— 比原本的即時 gzip-4 **少 20.3%**
（bundle 100.3→79.6KB、CSS 31.7→23.8KB），而且省掉每個請求現壓的 CPU（bundle 每次 3ms）。
⚠ 這條路的壞法**全都是無聲的**：①`.br` 過期 → 瀏覽器拿到**舊程式碼**，而 `?v=` 是即時算的、
看起來像已更新（且 immutable 一年，重整也沒用）②壓錯/截斷 → 整支 JS 解不開、白畫面
③Content-Type 與原路不一致（.js 必須是 `text/javascript`）④被 GZipMiddleware 重複壓。
判準＝**br 解壓後逐位元等於 identity**，一條涵蓋四種。已植入「過期的 .br」證明會失敗。
⚠ 另有一道執行期保險：來源 mtime 比 `.br` 新就**不送預壓檔**（放行走即時 gzip）——
沒有它的話，本機改完 JS 沒重啟就會安靜地送舊程式碼。
⚠ `brotli` 已在 requirements 明確宣告（原本只是傳遞相依）；沒有它會自動退回 gzip，功能不壞只是少 20%。

### 動到 `?v=` 資產版號、或在 `<script>` 裡組靜態網址後（守門員之十九）
```bash
node scripts/check_dup_assets.js   # 需本機服務跑著；約 40 秒
```
2026-09-13：`?v=` 改成**每支檔案各自的內容雜湊**（省下「部署後重抓整包」）之後，城堡圖在第一次載入
被抓了**兩次**（81KB × 2，就在關鍵路徑上）。根因＝預載那行寫在 `<script>` 裡，
**Jinja 會把變數輸出中的 `&` 轉成 `&amp;`** → 預載抓 `?v=…&amp;c=4`、`<img>` 抓 `?v=…&c=4`，
兩個不同網址。修法＝該處加 `| safe`（HTML 屬性裡不用，只有 script 內要）。
⚠ 這種壞法**完全無聲**：圖照樣顯示、零錯誤，只是白花一倍頻寬（修好後「看到 K 棒」快 0.3 秒）。
⚠ 判準要看**請求的網址**，不能看畫面 —— 畫面永遠是對的，這正是它難發現的原因。
已植回舊碼證明會失敗（拿掉 `| safe` → 立刻報「同一支檔案有 2 種網址」＋「網址帶著 &amp;」、回傳 1）。
★★ 判準必須是「**真的打到網路**」，不能只數請求次數（2026-09-14 修，第一版就錯）：
瀏覽器會為 favicon 再發一次請求，但那次是從**磁碟快取**讀的（CDP `fromDiskCache=true`、零傳輸），
service worker 命中快取時也一樣。只數次數 → 線上立刻誤報 favicon，
而且會把人引去做「favicon 改成獨立小檔」這種**反效果**的修法（實測反而多打一次網路 +6.5KB，已回退）。
⚠ 所以 favicon 就指 topbar 那張 `bear.png`：重用 `<img>` 已經下載的那份，別另外做小檔。

### 新增任何 `/api/_diag*` 或內部端點後（守門員之十五）
```bash
cd backend && ../.venv312/bin/python scripts/check_diag_auth.py   # 不需服務跑著
```
診斷端點是「開瀏覽器就能看」的除錯窗口，很好用所以會一直長出新的 —— 每支新的都可能忘記加
`_require_admin`，**忘了也完全沒有跡象**（自己開起來一樣好好的）。2026-08-16 稽核抓到：
`_diag_trade` 早就因為「會列出帳號名」擋起來，隔壁做同一件事的 `_diag_fvg` 卻全開 —— 它回
**帳號名稱＋各帳號掛單記錄**，而且每次呼叫都真的打 fapi 抓價＋逐帳號跑閘門（＝外人可以消耗
我們的 Binance 權重）。同批還有 `_diag_fugle`（拿我們每把金鑰各打一次富果，429 一撞台股即時
報價整批壞）、`_diag_mem`（快取鍵列出此刻有人在看哪些標的）、以及 `POST
/reset_pionex_cooldown` —— 全站**唯一**不需身分就能改伺服器狀態的端點，改的正好是限流保護。
⚠ `_require_admin` 是「設了 `ACCOUNT_ADMIN_KEY` 才擋」→ 本機開發完全不受影響，線上才生效。
⚠ 端點清單**從 OpenAPI schema 自動列舉**（路徑含底線開頭的段落＝內部），新增的自動涵蓋；
**不可以走 `app.routes`**：這版 FastAPI 的 include_router 會包成 `_IncludedRouter`（無 `.routes`、
`.path` 空字串）→ 只列得到 1 個、這支就「因為沒東西可測而通過」（我第一版就是，故另加
「列舉到 <5 個算測試不成立」的保險）。真的要公開就加進腳本的 `_EXEMPT` 並寫明理由。
已植回舊碼證明會失敗（拿掉 `_diag_fvg` 的守衛 → 立刻報「無金鑰 200」、回傳 1）。

### 動到 `utils/crt.py` 任何標記計算後（守門員之二十）
```bash
cd backend && ../.venv312/bin/python scripts/check_crt_golden.py          # 不需服務跑著，約 5 秒
cd backend && ../.venv312/bin/python scripts/check_crt_golden.py --update # 只有「刻意改標記邏輯」才可以用
```
多/空、破多空、順多空、特多空、FVG…是這個回測工具的**核心輸出**，錯了畫面完全看不出來（標記照樣在，只是少幾個或位置不同）。
判準＝K 線倉庫 **4h/1d 固定歷史區段**（5m 倉庫會滾動砍頭、不能用）× 參數變體的整份輸出雜湊，**逐位元**比對。
★ 效能優化（輸出不可變）一律跑這支。2026-09-17 把 4 個「逐根往後掃找觸碰」的迴圈改成稀疏表跳躍查詢（真實資料快 18~35%）就是靠它把關。
★ **注入 NaN 的變體必留**：植入「NaN 填 −inf」的錯，正常資料 6 組全過、只有 2 組 NaN 抓到 —— 資料空洞是跳躍類優化最容易分岔的地方。
⚠ 失敗時先問「我是不是刻意改了標記」：是 → `--update` 並在 commit 寫明哪些標記變了；不是 → 那就是 bug，**不可以用 --update 蓋掉**。
⚠ 回傳碼 2＝倉庫缺這些區段（測試不成立），不是通過。已植回兩種錯（門檻寫錯、NaN 處理錯）證明會失敗。
⚠ 勝率結果**格式**變了（加/拿掉鍵）要升 `get_crt_winrate` 的快取鍵版號（`crt_wrNNN`）：記憶體與 Redis 各留 30 分鐘，
不升會在部署後把舊格式送出去（2026-09-17 拿掉 smc_* 時漏升，隔輪補到 106）。

### 動到任何資料源／重採樣後（守門員之十七）
```bash
cd backend && ../.venv312/bin/python scripts/check_bar_invariants.py   # 需本機服務跑著；約 1 分鐘
```
K 棒本身錯了，上面所有 FVG／勝率／訊號**全部無效** —— 而畫面上完全看不出來。既有守門員各顧一塊：
`check_tf_spacing` 只驗「間隔對不對」、`check_bar_stability` 只盯 1m 即時那幾根、冒煙只驗一個組合；
**沒人驗過「每個市場每個時框拿回來的整段資料本身合不合法」**。判準都是硬不變式：
`low ≤ min(open,close) 且 high ≥ max(open,close)`（違反＝兩份快照縫出來的，沒有別的可能）／
`high ≥ low` ／時間嚴格遞增（重複的棒會讓回測算兩次）／沒有未來棒／無 NaN/inf/負值。
⚠ **不驗「有沒有洞」**：假日、停牌、永續上市日之前本來就有洞，驗了一定叫狼來了（那是
`repair_klines5m` 的守備範圍）。已驗證五種植入的壞資料全抓到、且「資料有洞」不誤報。
⚠ 時框清單**從 `config.js` 的 `TF_LABELS` 抽**，不寫死（我第一版寫死 8h/2h/30m → 9 個組合回 400，
印成一排「抓不到」）。
★ 間隔判準用**最小正間隔**不用中位數：美股一天只有 6.5 小時 → 4h 每天只有 2 根，盤中間隔 4h、
跨日 20h **各佔一半** → 中位數是擲骰子（實測 us/hk 4h 被誤判）。`check_tf_spacing` 註解寫著
「中位數判準通用」，但它從來沒測過股市的 4h —— 同 session 內相鄰棒必然剛好差一個時框，
最小間隔對 24 小時與有收盤的市場都成立。

### 動到 `/api/latest` 任何一條 return 後（守門員之十八）
```bash
cd backend && ../.venv312/bin/python scripts/check_latest_json_safe.py   # 不需服務跑著，約 5 秒
```
2026-09-10 從本機日誌挖出來的既存事故：**216 次** `POST /api/latest → 500`，例外是
`ValueError: Out of range float values are not JSON compliant: nan`。主圖每秒的即時輪詢、
自選那幾列的價格全走這支 —— 它一 500 就是使用者說的「K 棒停住、要重整才好」，
而前端兩個呼叫點都是 `if (!res.ok) return;` ＝**完全靜默**（畫面零錯誤、零提示）。
⚠ 根因是**結構性**的不是某一行寫錯：`_get_latest_impl` 有 8 條 return，只有走
`df_to_records()` 的那幾條會把 NaN 換掉，另外幾條是**手工組 dict**（台股 MIS 日線／
MIS 補號平盤棒／yfinance 單根／Finnhub、騰訊累積器）—— 上游一顆 NaN 直接進回應。
→ 消毒移到**邊界**（`_scrub_latest`），第 9 條 return 也不可能漏。
⚠ **壞棒是丟掉不是補 None**：K 棒少了價格就不是 K 棒，送 null 只是把爆炸點搬到前端。
⚠ 那份 traceback **一行我們自己的程式都沒有**（錯在 FastAPI 序列化回應時）→ 事後查不出
是哪一檔哪個時框，所以 `_scrub_latest` 一定要把丟掉的棒印出來（每組合只印一次）。
★★ 判準必須用 `json.dumps(..., allow_nan=False)`（starlette 就是這樣序列化的）：
用預設的 `allow_nan=True` 測，NaN 會被寫成非法的 `NaN` 字面值卻**不報錯** ＝ 假通過，
我第一版就這樣，差點得出「植回舊碼也沒事」的結論。
已對 6 條路徑各自注入 NaN 並確認植回舊碼會壞（注入沒生效的情境回傳碼 2＝測試不成立）。

### 錯誤回應的 body 也是 JSON —— `.json()` 成功不代表拿到答案（2026-08-20）
天氣卡在 `/api/nearby_rain` 回 503 時，畫面上寫的是 **「☀️ 你這和附近都沒下雨」** ——
一個很有自信的**肯定句**，而我們其實根本不知道。根因：`fetch(...).then(r => r.json())`
沒看 `r.ok`，503 的 `{"detail":...}` 照樣解析成功 → 那份「沒有 raining_here／cells／nearest」
的東西掉進最後的預設分支。
→ ①`r.ok` 才 `.json()`；②文字函式開頭先擋「取不到」；③有舊值就留舊值（10 分內的雨況仍有參考
價值），但**絕不讓失敗變成一句「都沒下雨」**。
★ 通則：**只要 fetch 沒檢查 `r.ok`，錯誤回應就會被當成一份合法答案** —— 全站掃一次這個模式。

### 動到指標計算後（守門員之三十三）
```bash
cd backend && ../.venv312/bin/python scripts/check_indicator_math.py   # 需服務跑著，約 20 秒
```
2026-09-26：RSI／MACD／KDJ／布林是畫在圖上、**也餵進策略閘門**的東西，算錯的形狀全是靜默的
（線照樣畫、數字照樣跳，只是值不對，而所有以它為依據的判斷跟著一起錯）。
既有守門員沒人管這塊：`check_crt_golden` 鎖的是**標記輸出的雜湊**（公式一改它會紅，
但只說「輸出變了」，不會說哪個指標算錯）、`check_bar_invariants` 只管 K 棒本身。
判準＝端點實際回的欄位 vs **獨立寫一次的參考實作**逐點比（Wilder RSI、EMA adjust=False、
KDJ α=1/3、SMA±2σ），容許 **0.005%** 的傳輸四捨五入（實測最大 0.0005%）。
已植回兩種壞法證明會失敗：RSI 改簡單均值（相對差 76~100%）、布林改 ddof=0（0.12%），皆離開碼 1。
**查證結果：四個指標的公式全部正確**（差異只有 1e-5 等級的傳輸四捨五入）。

★★ **布林的標準差慣例是個「已知且刻意」的差異，不是 bug**：
目前用 pandas 預設的 **ddof=1（樣本標準差）**，而 TradingView 等看盤軟體用 **ddof=0（母體）**
→ 我們的通道系統性**寬 2.53%**（BTC 4h 實測；上軌在 84000 價位差到 107 點）。
⚠ 它**不只是畫面**：`utils/crt.py` 用「碰到上下軌」當 FVG 進場閘門之一、`notify_monitor.py`
  也讀它 → 實測 1580 根 4h，換成 ddof=0 會讓碰軌判定變動 **46 根（2.9%）**。
  換慣例＝**改變回測輸出與通知觸發**，是產品決策不是修 bug。
  真要換：同步跑 `check_crt_golden.py --update`、更新本守門員的慣例鎖、並在 commit 寫明。
  守門員已把現行慣例鎖住（改成 ddof=0 會被指名點出後果），確保哪天要改是「有意識地改」。

### 動到天氣數值/日夜判斷後（守門員之三十二）
```bash
cd backend && ../.venv312/bin/python scripts/check_weather_calc.py   # 需服務跑著，約 40 秒
```
2026-09-26 使用者：「幫我檢測天氣部分計算都沒問題」。既有兩支只顧別的面向
（`check_weather_swr` 驗「過期先回舊值」、`check_rain_eta` 驗雨區幾何）——
**沒有人驗過「回給前端的那些數值本身對不對」**，而天氣壞掉的形狀全是靜默的。
實際抓到四個（全都在 `_from_omt`，＝**非台/港/日的使用者才會遇到，自己測永遠測不到**）：
① ★★ `is_day = int(c.get("is_day") or 1) == 1` —— **0 是合法值**（夜晚就是 0），
   `0 or 1` 讓 Open-Meteo 來源**永遠白天**。實測紐約當地 23:27、倫敦 04:27 都回 `is_day=True`
   → 晴朗的夜晚會被畫成白天的天空。
② `temperature_2m or 20` → 剛好 0°C 變成 20°C。③ `visibility or 10000` → 濃霧 0m 變成 10 公里。
④ `"humidity": 0` 寫死、參數也沒要 `relative_humidity_2m` → 那些使用者的濕度**永遠 0%**。
→ 收斂成 `_omt_num()`：只有 `None` 才用預設，0 照收。
★★ 通則：**`x or 預設` 會把合法的 0 當成「沒有值」** —— 同 `Math.max(0, NaN)` 擋不住 NaN、
  `fetch` 沒看 `r.ok` 會把錯誤當答案，都是「看起來有防呆、其實把合法值吃掉」。
判準（一律問端點實際回了什麼）：四個來源都要驗到／數值在物理範圍／**`is_day` 必須與
「當地此刻是否在日出日沒之間」一致**（晨昏各 25 分寬容）／日出早於日沒／`night` 只能在 is_day=False／
`tz_offset_min` 等於 zoneinfo 的真實偏移（含日光節約）／**濕度不可整組來源皆 0**。
已植回①④證明會失敗（紐約/倫敦 is_day 不一致 ＋「openmeteo 濕度整組都是 0」，離開碼 1）。
⚠ 回傳碼 2＝測試不成立（服務沒跑／四個來源有人整個取不到）。

**同批查證過、確認沒問題的（省得下次再花一輪）**：
・日出日落：端點其實是拿 **Open-Meteo 的天文值**（`sun_src=open-meteo`，含真實時區與日光節約），
  `_sun_times_local` 只是備援；備援公式本身與權威值差 **≤4 分鐘**（註解寫 ±2 稍樂觀），
  但它的 `round(lon/15)` 時區近似在**日光節約區與政治時區**會差 0.5~2 小時（8 城市錯 4 個）——
  只影響「Open-Meteo 掛掉」的降級情境。
・月相：與 2026 年五次朔、五次望對照，最大偏離 **1.1 天**（多數 0.1~0.5 天），畫月亮夠用。
・大圓距離 `_kmBetween`：與獨立球面式差 **0.0001%**。
・`_bearing8` 改成**大圓初始方位**（原本是等距近似）：近距離幾乎不變（台北周邊 1500km 內
  最大差 3.15°、2.67% 會落到相鄰格且都在分界線上），但遠距離原本整個錯 ——
  實測紐約看西太平洋颱風算成「正東方 12684km」，真方位是往西北經北極。
・前端 `_locNowMin()` 正確吃後端的 `tz_offset_min`（經度近似只是備援）；`_cloudClr` 的 NaN 防呆還在。
・太陽弧線 `_sunArcPos`／畫太陽那段原本寫 `(now-rise)/(set-rise)`，**沒處理日落跨午夜** ——
  高緯度夏季（雷克雅維克 6 月：日出 02:55、日落 00:04）分母變負、夾限後太陽整天卡在地平線。
  旁邊的 `_moonProg` 早就處理了跨午夜，**同一個檔案裡兩套寫法**。→ 抽成共用的 `_dayProg()`，
  回 null＝現在不是白天（呼叫端據此決定畫不畫）。極晝（rise===set）回 0.5 停在天頂。
・`forecast.now.feels` 在台/港/日**刻意是 null**（註解寫明：官方觀測沒有體感溫度，
  清掉 Open-Meteo 的模式值避免蓋過官方）——不是漏抓，別「修」它。
・`forecast.today.pop`（CWA 官方 12 小時粒度）與 `today.afternoon.pop`（Open-Meteo 逐小時）
  來自**不同來源**，數字可以不一樣（實測 0 vs 4）；兩邊都是誠實值，不做 max 綁定
  （綁了就會污染官方 PoP，違反「降雨機率儘量用當地氣象署」那條）。
・`/api/nearby_rain`、`/api/typhoon` 的實際回應都驗過：颱風距離手算 625km vs 回報 620km、
  台北看它是東南 116°，都對得上。
・地名：Nominatim 會回「简体;繁體」多語名（實測紐約 city="纽约;紐約"）→ 只取第一段；
  中英之間補空格（原本黏成「紐約New York County」）。
  ⚠ 取第一段拿到的是簡體，`name:zh` 欄位本身就這樣存 —— 要改成繁體只能猜，未動。

### 天氣 SWR 守門員的等待時間不可寫死（2026-08-20）
`check_weather_swr.py` 固定等 4 秒就檢查 busy 旗標，但背景重算就是再跑一次冷路徑 ——
實測 `/api/weather` 冷取 5519ms → 4 秒必然還沒跑完 → **每次都報「busy 沒清乾淨」＝叫狼來了**
（同一輪 `nearby_rain` 冷取 8118ms 反而通過，因為它的測站清單已被上一趟快取住）。
★ 「兩支結果不一致、而且慢的那支反而通過」就是判準寫死時間的典型症狀。
→ 改成等 `max(6s, 冷取時間×2 + 3s)` 並輪詢，程式本身沒問題（`finally` 早就有 `discard`）。

### 「算不出來」不可以長得像「這裡沒有訊號」（2026-08-20）
實測把 `/api/crt_winrate` 擋成 503：標記全清成 0、勝率 HUD 顯示「—」、畫面上**沒有任何其他跡象**
→ 跟「這個標的真的沒有訊號」完全分不出來。對回測工具來說這兩件事意義天差地遠，而且正是本檔
一直在講的最危險形狀（靜默地給一個看起來正常的答案，同 `行情中斷` 那條的理由）。
→ 真失敗（非 abort/非被新請求取代）時 HUD 改寫「訊號計算失敗」＋ `title` 說明原因；
成功時要把 `title` 一起清掉（否則舊的說明會一直掛著）。
⚠ 2026-09-17 勝率欄整條刪除（使用者：「上方勝率的東西也刪除」）→ 失敗提示搬到上方左側 `#wrFailNote`（平常空白、`:empty` 不佔位），
在 `_fetchWinRateNow` **開頭**就清（快取命中那條會提早 return，清在後面會留下假的失敗字樣）。
`#wrStatus`（筆數）等 HUD 元素都已不存在，winrate.js 對它們一律是 null 安全寫法——新增讀寫 HUD 的程式前先確認元素還在。
⚠ 不用提示框 —— 使用者明確表示操作類提示不要；這是**狀態**不是事件，就地寫清楚即可。
⚠ `Failed to fetch` 被歸類為 abort-like（快速連切標的會刷一排 AbortError 是預期的），
所以整段網路斷掉時**這裡是靜默的** —— 那條由 `netOffline`／`tkStale` 負責，不重複報。

### 分頁在背景時應該是**零流量**（2026-08-19）
實測前景 3.75 KB/秒 → 背景 **0.00 KB/秒**。輪詢本來就會在背景停掉，但離線偵測原本不知道這件事：
它看「報價心跳 `_tkLastOkTs` 停超過 12 秒」當可疑訊號 → 背景時心跳必然停 → **每 4 秒探測一次
`/static/manifest.json`，整天開著就整天在打**（實測背景 60 秒打 12 次，是背景期間唯一的流量）。
修法＝`_tick()` 在 `document.hidden` 時直接 return。使用者沒在看，就算真斷線也不需要現在告訴他；
回前景後輪詢恢復，12 秒內自然判得出來。
★ 通則：**任何「靠心跳停了就判定異常」的偵測，都要先排除「是我們自己刻意停的」**。

### 切換市場分頁不可以殘留上一個市場的價格（2026-08-19）
`.tk-seg-btn`（排序切換）本來就會 `renderTickers()` 立刻重畫，`.tk-mkt-btn`（市場切換）**漏了**
→ 只發非同步 fetch，回應到達前畫面上留著**上一個市場**的列與價格（使用者：「切換有短暫的價格
沒切換」）。本機幾十毫秒看不出來，慢網路下實測整整 2.4 秒。
修法＝切換後立刻 `renderTickers()`；同時把台股那份也存進本機快取 `_tc`（原本只存合約+現貨）
→ 切過去 **+0ms 就有底稿**，不會空白。⚠ 只有真的用過台股才存（只看合約的人不必付那 1959 檔的空間）。
⚠ 沒有底稿時寧可**空一下**也不要顯示別的市場的價格 —— 使用者會以為那是這個市場的行情。

### 報價流量現況與「別抓沒人讀的東西」（2026-08-19 重量）
只看合約的使用者靜置 60 秒：`futures` 60 次 / 168KB＋`latest` 60 次 / 55KB ＝ **3.72 KB/秒**
（本輪之前 6.5 KB/秒）。三項改動：
① 差量細到欄位級（−55%，見守門員之十六）
② **現貨心跳改成「用過才保鮮」**：現貨清單全站只有兩個消費者（搜尋視窗的現貨分頁／主圖看的
是現貨標的），只看永續的人一輩子讀不到它 —— 原本卻**無條件**每 15 秒打一次（9.7KB/分鐘純浪費）。
改成用過才保溫 10 分鐘。⚠ 不保溫也不會壞：切到現貨分頁時 `_spotLastTs=0`＋立刻 fetch。
③ 心跳若要抓就走差量：原註解寫「差量省不到、直接整包最穩」——那是**列級差量時代**的結論，
欄位級之後 15 秒間隔實測 6.3KB → **1.87KB（−70%）**。
★★ **註解裡「量過、不划算」的結論會隨相依的東西改變而過期 —— 引用它之前先重量一次。**
⚠ 低頻抓法要有自己的整包自癒節奏（現貨每 20 次≈5 分鐘、跨市場每 40 次≈2 分鐘）：
主輪詢的「每 60 輪整包」是別人的計數器，低頻路徑輪不到 → 沒有任何機會被校正。

### 自選列**沒有 `data-display`** —— 選擇器寫錯會整批跳過（2026-08-21，同一坑第二次）
`_buildWlRow` 只給 `data-mkt`／`data-exch`／`data-sym`。`_updateTickerPrices` 用
`.ticker-item[data-display]` → **整個自選分頁的列全部不更新**，只能等別的路徑偶爾重畫。
症狀是**你正在看的那一列**卡住好幾秒、跟其他列對不上（使用者：「點進 A 再點 B，A 的會卡住
回到之前」）—— 實測那列 DOM 2317.85 / 來源 2318.41，且 +500ms 與 +3000ms 是同一個數字，
而沒在看的列 DOM 與來源**完全相等**。
★ 旁邊 `_tkSyncChartRow` 的註解**早就記過這個坑**，上面那支卻沒跟著修 —— 同一個檔案裡踩兩次。
→ 一律用 `.ticker-item` ＋ `d.display || d.sym` 取鍵（兩支現在一致）。
⚠ 判準要比「DOM 數字 vs `_tickerData` 數字」，不是「有沒有在動」：整個清單看起來都在跳，
只有那一列不對，數變動的話看不出來。

### 「現價」只能有**一個**數字（2026-08-23 使用者：「我要一致，且都是最新」）
同一檔的現價本來有兩個來源，**三支同時取樣**實測**兩個都準到 0.05 點**（BTC 76,000 上）：
`/api/latest` 形成中那根的收盤 ／ `/api/tickers` 的最後成交價。誰也不比誰準，
但**顯示兩個數字就會看到跳動**（切標的時那一列換來源）。
→ `_pushCurPrice(key, price, ts)` 收斂成一個：誰的 `ts` 新就用誰，
**同一次呼叫同時寫進主圖現價線與行情列那一列** → 結構上不可能不一致。
實測 266 次取樣（每 150ms／40 秒）現價線 == 那一列 **266/266（100%）**，且仍貼著幣安真值。
⚠ 換標的要 `_resetCurPrice()`，否則會把上一檔的價餵給新那一列。
⚠⚠ **比較「誰比較準」時，三支一定要同時發**：我第一版依序打（間隔 1.4 秒），
量到「主圖落後 17.9 點」——那是**價格自己在動**，不是延遲。同時發之後兩者都是 0.0x 點。
**拿會變動的量做比較，取樣時刻不同就等於沒有比較。**

### 動到「主圖價餵給行情列那一行」後（守門員之三十一）
```bash
node scripts/check_ticker_no_rollback.js   # 需本機服務跑著；約 3~5 分鐘
```
2026-09-25 使用者：「當我按標的 a 切回標的 b，b 的合約行情價格會跳回上一秒之前的再回來」。
根因是**兩件各自看都正常的事湊起來**的：
①`/api/ohlcv` 的 limit 查詢有 **30 秒 TTL 快取** → 切走再切回同一檔吃到快取，而最後那根是
**形成中**的 K 棒 → 它的收盤是當初抓的（實測 ARK 1m 等 24 秒：ohlcv 末根 0.2389／同刻
`/api/latest` 0.2379，**差 0.42%**；BTC 上實測 **76 點**）。
②前端 `_mainChartPrice` 無條件拿主圖末根當「現價」寫進那一列（`_paintTickerRow` 還會
`t.price = _mp` **寫回資料**）。→ 畫面上就是「跳回幾秒前的價，下一拍再回來」，**零錯誤、零跡象**。
⚠⚠ 原本那道年齡閘門擋不住：它看的是**棒的時間**（1m 就是整分鐘、日線是今天 00:00），
**量不到「這批資料是幾點抓的」** —— 30 秒前的價照樣過關。
→ 後端 `/api/ohlcv` 回 `ts`（**放在 `cache.set` 之前** → 快取命中時回的是當初那一刻＝講實話），
前端只有 **1.5 秒內**才准拿末根當現價；不新鮮就回 null → 那一列用**自己的**即時報價
（1 秒一次，永遠不會往回走），而主圖 1 秒內就有 `/api/latest` 推進 `_curPx`、之後照樣由主圖領軍，
**不留空窗**（實測穩定狀態 現價線 == 那一行 60/60）。
★★ 判準踩過**四個**坑，每個都會讓它說謊：
  ①**不可以拿 `_tickerData` 當參照** —— `_paintTickerRow` 會把主圖價寫回那一筆＝自己測自己
    （第一版量到 0.0000% 全過）。參照一律取網路上的 `/api/tickers` 回應。
  ②**不可以用「這個數字上次出現在幾秒前」判斷** —— 小數 4 位的幣價只在幾個檔位之間來回，
    「現在顯示的剛好也是 5.7 秒前出現過的」是巧合（第二版誤報）。要鎖定**兇手本人**：
    這次那份 ohlcv 回應的末根值。
  ③**標的要照「量」排序取第一檔**（BTC 等高價流動好的）—— 小幣一個跳動就佔 0.04%，
    比要偵測的偏離還大，量到的全是量化雜訊；門檻另外依實測跳動值自動放大。
  ④ohlcv 的新舊**從網路回應量**（後端回的 ts），**不可以讀 `window._ohlcvTs`** ——
    植回舊行為的手法就是去蓋那個變數，讀它等於量到自己灌的值（那一版三輪全印「重抓的」）。
⚠ 快取命中是**隔輪才成立**的（命中不會刷新 TTL）→ 沒命中的那輪不算數、自動重跑。
⚠ 回傳碼 2＝測試不成立（沒吃到快取／那幾輪價格剛好沒漂），不是通過。
已植回舊行為證明會失敗（顯示 26.4 秒前的 84615.6、當下行情 84539.2，差 0.090%＝76 點）。
★ 通則：**「這根棒是什麼時候的」與「這批資料是什麼時候抓的」是兩件事** ——
  任何拿快取資料當「現在」的地方，都要問後端要取樣時刻，別用資料自己的時間欄位推。

**同批（2026-09-25）：`startRealtime()` 改成「開始輪詢就先發一次」。**
`setInterval` 的第一發在**一個週期之後** —— 實測切標的後 **1060ms 才送出、1132ms 才到貨**，
那段時間主圖的現價線與形成中那根吃的就是上面說的那批舊 ohlcv（台股/美股週期 5 秒＝更久）。
改成先發一次後實測 **1132ms → 201ms**。成本＝每次切標的多一發 `/api/latest`，而它後端有
1 秒 TTL＋單飛；三個呼叫點（切標的／切回前景／離開重播）都受惠，`fetchLatest` 自己開頭
就有 replay／離線／標的脈絡三道守衛。
⚠⚠ **這項優化會讓上面那支守門員的「植回舊行為」重現不出來**（暴露窗口 1.1 秒 → 0.2 秒，
  實測 3 輪全 0、整支回傳 2）→ 植回時必須**連 `/api/latest` 一起擋掉**才是當年真正的窗口。
  ★ 通則：**優化縮短了某個 bug 的暴露窗口時，守著它的守門員要跟著調植回手法**，
    否則它會從「抓得到」安靜地退化成「永遠測試不成立」。
⚠ 那支的偏離門檻也踩過：訂 0.02% 時 BTC 一安靜（20 秒漂不到 17 點）就連舊行為都重現不出來
  → 改成 `max(0.005%, 3×實測跳動)`，並且**標的挑「量前四名裡這 12 秒真的在動的那一檔」**。
  真正擋住誤報的是兩道因果條件（顯示值正好等於那批舊 ohlcv 的末根＋行情來源最近 3 秒沒報過），
  門檻只是排掉「舊值剛好等於現在的價」那種無害情形。

### 報價列比主圖慢半拍 —— 是**取得方式**不同，不是資料差（2026-08-23）
使用者：「感覺合約行情數據沒主圖快」。兩支端點現在都回 `ts`（這份資料是幾點抓的），實測
**主圖比報價列新、中位 0.78 秒**。原因：
・`/api/latest`（主圖）＝**你要的時候現抓**（1 秒 TTL＋單飛）→ 資料年齡 ≈ 0
・`/api/tickers`（報價列）＝背景 worker **每秒推一次快照** → 輪詢落在隨機相位，平均慢半拍
→ 前端輪詢由固定 `setInterval` 改成**自我排程**：用回應的 `ts` 算陳舊度，把下一次排在
「伺服器剛更新完」之後 120ms。實測陳舊度 **中位 241ms／p90 338ms**（原本隨機 0~1000ms）。
⚠ 時鐘偏差：`Date.now() - ts*1000` 含本機與伺服器時鐘差，不能直接當年齡 →
取**最近樣本的最小值**當基準（那筆最接近「剛更新就拿到」），偏差自然抵銷。
⚠ 一定要夾限（下限 250ms、上限 1.5 倍週期）：算錯也不能變成狂打或停擺。
⚠ 換成 setTimeout 後，`clearInterval` 的三個呼叫點都要改 `clearTimeout`，
且自我排程要檢查 `_tickerTimer !== null` 才續排，否則 `stopTickerRefresh` 停不住。

### 切標的時，現價標線會留著上一檔的價（2026-08-23）
使用者：「主圖點進去，會有一瞬間最新價格標線不對」。`updateLatestPriceLine` 只在 `renderCandles`
拿到新資料時才更新 → 從點下去到新資料到達，標線與右軸標籤留著**上一檔**的價。
實測本機約 **80ms** 顯示上一檔的 77,311.6（目標 2,425）；慢的機器/網路窗口更長。
→ `loadData` 偵測到**換標的**（比對 `_chartDataKey` 前後值）就先 `hideLatestPriceLine()`；
不必復原，新資料一畫上去 `renderCandles` 就會重建。⚠ 換時框不收（同一檔，價仍有效）。
⚠ **判準要問「標線」本身**（`latestPriceLine.options().price` ＋自訂 DOM 標籤），
不可問 `candleSeries.data()` —— 序列資料在切換時本來就還留著舊的，量它會永遠是紅的
（我第一版就是，修好了也照樣報失敗）。已植回舊碼證明會失敗。

### 拿「主圖最後一根」當現價，**必須先確認那根真的是「現在」**（2026-08-23）
使用者：「合約行情差很多」「畫面位置記憶在歷史時，要動一下主圖才會有最新價格」。
`_mainChartPrice` 無條件用 `ohlcvData` 最後一根的收盤當現價，餵給「你正在看的那一列」。
但**捲在歷史時載入的是「錨點附近的有界視窗」**（`window._hasFwdGap=true`）→ 最後一根是
一個月前的收盤。實測重整後停在 2026-07-27：行情列顯示 **65,375.1**、真實行情 **77,300.1**，
**差 15.43%**（不是零點幾點，是一整段行情）。
→ 兩道閘門都要：①`window._hasFwdGap`（有界視窗，資料本來就沒到現在）
②「最後一根不能太舊」（即時輪詢中斷／補載還沒追上時沒有旗標可看）。不合格就回 null，
那一列退回用行情來源的即時價。實測 15.43% → **0.00%**，而看最新時仍然跟著主圖走。
⚠ **`toTime()` 回的是圖表時間（已 +8 小時），不可直接跟 `Date.now()` 比** —— 我第一版就是，
age 永遠差 8 小時 → 判定永遠不通過 → 等於把整個功能關掉。而且症狀**很像修好了**：
那一列改用行情來源、數字剛好對得上，只是主圖同步沒了。

### 切回某個市場分頁時「先顯示舊價」（2026-08-23）
使用者：「點進合約行情後，數值會先跳錯的」。跨市場保鮮原本**只在自選裡有該市場的標的**時才跑
→ 自選沒有加密的人待在台股分頁時，`_tickerData` 整段凍住；切回合約那一刻先畫**凍住的舊價**、
下一輪才跳成對的。實測待在台股 25 秒後切回，切換當下最大偏離 **0.395%**。
→ 條件放寬成「自選裡有它 **或** 這次工作階段用過這個市場分頁」（同現貨「用過才保鮮」的原則：
沒用過的市場一格流量都不花）。實測待在台股 30 秒，加密 rev 持續前進、6/6 取樣都在更新。
⚠ 判準不可拿「幾秒後的值」當正確答案：小幣本身 3 秒就會動 0.2%，會把真實波動算成 bug
（我第一版就這樣，量到 0.201% 以為沒修好）。要問的是「**那份資料有沒有在更新**」。

### 自選是**混市場**清單 —— 別假設「另一個市場有人在更新」（2026-08-19）
報價輪詢一次只抓「目前分頁那個市場」，但自選同時放著加密/台股/美股/港股。
使用者：「我在台股所以他不動，這樣很怪，要都可以動」—— 停在台股分頁時 `_tickerData`（加密）
沒有任何人更新 → 自選裡的加密價格全部定格，**而且完全不報錯**。
⚠ `_refreshWlPrices` 沒接住是因為它**刻意跳過 crypto**，註解寫著「加密那些本來就跟著每秒的
報價輪詢在跳」—— 那個前提只在「人在加密分頁」時成立。**註解裡的前提要當成會過期的東西看。**
修法＝`_fetchCrossMarket()`：另一個市場只要自選裡真的有它的標的就順便抓一份差量
（節流 3 秒＋走差量，一次約 1~2KB；沒有的話完全不打）。
同批修：自選的**台股**改成優先讀台股清單（它本來就有價/漲跌幅），原本一律走 `_wlPriceCache`
逐檔打 `/api/latest` —— 但收盤後它只回 **1 根**，而那條路要求 `>= 2`（要前一根算漲跌幅）→
什麼都不存 → 自選的台股永遠 `---`，即使清單裡明明有 2380.0。`>= 1` 就收（漲跌幅拿不到留空）。

### `Math.max(0, NaN)` 回傳的是 **NaN**（2026-08-22）
`dSunny` 的 `const clr = Math.max(0, 1 - _wd.cloudCover/100)` —— 看起來有防呆，其實擋不住任何東西。
`cloudCover` 只要是 undefined/null（天氣還沒載到、或那個來源沒給雲量），clr → NaN → 每個
`rgba(...,${NaN})` 都被 canvas 拒絕，**每一幀丟一次 DOMException**（實測 95 秒 **1328 個**）。
畫面上只是光效不見、零提示，卻會把守門員的「零 JS 錯誤」判準整個淹掉。
→ 收斂成 `_cloudClr()`：`Number()` + `isFinite()` 才回值，量不到就當晴朗。
★ 通則：**用 `Math.max/min` 夾限不等於防呆** —— NaN 會穿過去。要擋就先 `isFinite()`。

### 「直接打交易所」的 fallback 一定要 TTL＋單飛（2026-08-21）
`/api/tickers` 的冷啟動 fallback 原本直接 `fetch_tickers(market)`，沒有任何快取 —— 因為它
「只在冷啟動那幾秒會走到」。但同日改成「共享快照過期＝視同無資料」之後，一個卡住的 follower
會**持續**落到這條路，而前端是**每秒**輪詢：每個請求打一次 fapi 全市場 24hr（權重約 40）
→ 單一使用者就吃掉近半的每分鐘權重預算，直接把 Binance 打進冷卻。
→ 2 秒 TTL ＋ 單飛（TTL 擋不住「同時 miss」的叢發，同 `/api/ohlcv` 的教訓）。
實測：TTL 內第 2 次 0.0ms、上游只打 1 次；10 條同時 miss → 上游也只打 1 次。
★ 通則：**改動「什麼情況會走到某條路」時，要重新檢查那條路的成本假設** ——
「它很少被走到所以不用快取」這種前提，會被你自己的另一個修改推翻。

### 共享報價快照**過期就不可以再端出去**（2026-08-21 事故）
使用者：「線上整個合約行情會卡住」。線上 workers=2，取樣 24 次發現**兩個 boot id、兩份資料**：
`pid 6 = leader` → 696 檔、rev ~350，一直在動；`pid 5 = follower` → **1255 檔、rev 3200，凍住的**
（boot 比 leader 早 12.6 小時）。前端每秒輪流打到兩個 worker，一半的回應是**十二小時前的快照**，
而且 `source` 照樣寫 "live"、HTTP 200、筆數還更多 → 畫面上就是價格一直彈回舊值。
根因：`_read_shared()` **讀到什麼就回什麼，從來不看那份資料多舊**。共享快照在磁碟上，
leader 換人／寫入失敗／檔案是上一輪容器留下的，follower 都會照樣把它當即時資料端出去。
→ 超過 `_SHARED_MAX_AGE`(30s，leader 每秒寫一次) 視同無資料，`has_data()` 回 False，
`/api/tickers` 自動退回直接打交易所（慢一點，但**是真的**）。過期時每分鐘最多印一次警告。
★★ **寧可回空也不要回舊**：舊快照有筆數、有價格、不報錯，是最難發現的一種壞法。
⚠ 診斷這類問題先看 **boot id 的分佈**（`rev` 的冒號前那段）：一個 boot ＝一個來源，
出現兩個就代表你的請求被兩份互不相干的狀態輪流服務，比對「筆數」更快看出來。

### 動到報價列差量（`/api/tickers`）後（守門員之十六）
```bash
node scripts/check_ticker_delta_fields.js   # 需本機服務跑著；約 2 分鐘
```
報價輪詢是**全站最高頻的請求**（crypto 每秒一輪／人）。2026-08-17 差量從「這個標的變了就整列
重送」細到**只送真的變了的欄位**（`_track` 的 `sym_rev` 由 `{sym: rev}` 改成 `{sym: {欄位: rev}}`）
→ 實測 **−55%**。動機：crypto 每秒有 672/689 檔在動，列級差量幾乎等於整包（17.4KB vs 17.9KB），
但一列 6 個欄位裡 `symbol`/`display` **永遠不變**（佔 40%）、`open` 一整天不變（13%）。
⚠⚠ **必須由前端帶 `fd=1` 明講「我看得懂」，後端預設回舊格式（整列）**。2026-08-19 使用者回報
「合約行情不動了」就是漏了這層：他的分頁還跑著改版前的 JS（`_tkMerge` 是**整列覆蓋**），後端卻
已經在送部分欄位 → `symbol`/`open`/`volume` 被洗掉、排序崩掉、畫面凍住，**而且零 JS 錯誤**。
★ 全新 profile 永遠測不到（它拿的是新 JS）—— 但線上每次部署都會有一批「開著沒重整」的分頁中招。
**通則：改動「後端送什麼、前端才看得懂」的格式時，一律由前端宣告能力、後端預設走舊格式。**
⚠ 差量是相對**客戶端的 token**（不是伺服器的上一版）→ 每個欄位都要各自記最後變動版本。
⚠ 前端 `_tkMerge` 的 delta 分支必須是**合併** `{...old, ...t}`，不可覆蓋（覆蓋＝沒送來的欄位消失，
而 symbol/open 不再送＝清單變空殼）；`_tkFill` 要在**合併之後**（它算 change_amt 需要 price＋open
兩個）；推導欄位（change_amt/spot）只在**後端沒送**時才作廢重算 —— 台股的 change_amt 是對前一日
收盤算的、由後端送來，清掉會被錯誤公式蓋掉。
判準＝跑滿 **>60 輪**（跨過整包自癒）後，把前端手上的清單跟**同一刻的整包**逐欄位比：慢變欄位
（symbol/display/open/volume）必須完全相同，每秒在跳的（price/change_pct）只驗是數字。

### 「上次看的畫面」跨裝置同步（2026-08-17）
`lastSymbol`（標的/時框/縮放/看到哪個時間段）已從 `_PULL_SKIP` 拿掉 → 跟著帳號快照跨裝置。
⚠⚠ **下行必須比時間戳，不可讓雲端無條件蓋掉本機**（2026-08-20 使用者：「刷新後會縮小」）。
`lastSymbol` 在 `_ACCT_NO_TOUCH` 裡＝**不觸發推送** → 雲端那份常常是舊的；原本開機無條件套用，
就把使用者剛調好的縮放換成雲端的舊值（舊資料若沒有 `barSpacing`，還會退回 `fitContent` 把整段
擠進畫面＝更小）。→ `saveLastSymbol` 寫 `ts`，下行只有 `remote.ts > local.ts` 才套（無 ts 視為最舊）。
★ 通則：**「不主動推送」與「下行無條件覆蓋」不能同時存在** —— 一定要有誰比較新的判準。
⚠ 它同時被加進 **`_ACCT_NO_TOUCH`**：它每次平移停手 1.2 秒就寫一次，若跟著觸發 `_acctTouch`，
2.5 秒的 debounce 會被一直重新計時、`_acctFlush` 幾乎不會執行 ＝**整包雲端同步等於失效**
（`_tc` 當初就是踩這個才被加進 `_ACCT_SKIP`）。放 NO_TOUCH＝照樣進快照，但等別的變更或切背景
flush 時才上去。⚠ 下行**只在開機那一次**套用（切回前景也套的話，另一台一動你這台的圖就被扯走）；
且必須 `loadLastSymbol(true)` 跳過網址（網址的 ?s=&tf= 是這台自己 replaceState 寫的，會蓋回雲端值）。
⚠ 測這個功能**一定要用假帳號名**（見 memory `feedback_never-use-real-account-in-tests`）；
本機沒設 `ACCOUNT_ADMIN_KEY` 開不了 `admin/create` → 直接往 `backend/.accounts.db` 插一列、測完刪掉。

### 動到「重整後回到上次的畫面」後（守門員之十四）
```bash
node scripts/check_view_restore.js   # 需本機服務跑著；約 2 分鐘
```
使用者 2026-08-16：「記憶上次看的畫面，包含大小跟時區，重整後要一樣」。這條路有**三個各自
獨立、都靜默失效**的環節，缺一個畫面就回不去 ——
①**存**：`lastSymbol` 只存 barSpacing ＋「距最新棒幾根」→ 只描述得了「貼在最新」的畫面；
捲回 7/20 重整就跳回今天。改成捲在歷史時另存右緣的**時間** `anchorT`。
★ 錨點一律問 `getVisibleRange()`（直接回時間），**不可以拿 logical index 去索引 `ohlcvData`**
（index 指的是畫在圖上的序列、`ohlcvData` 是來源，兩者長度不同 → 實測把「看 7/20」存成 4/30）。
②**讀**：`loadLastSymbol()` 原本「網址有 s/tf/m 就丟掉本機視角」，但 `_syncUrlState()` 每次切
標的/時框都 replaceState 把 s/tf/m 寫進**自己的**網址 → 重整時這條件**永遠成立**＝連縮放還原
都從來沒生效過。改成比對「網址指的是不是同一個畫面」。
③**還原後守住**：`_placeAtAnchor` 用邏輯索引只設一次，之後背景補載往 `ohlcvData` 塞進幾千根
舊棒（實測 420→14,040）→ 同一個索引指到的時間整個位移，還原到 7/22 的畫面幾秒後自己跑到
**2025-09-01**（差 11 個月）。修法＝`_holdAnchorByTime()` 按時間重申 8 秒、使用者一動就放手。
旁邊的 `_guardRestore` 救不了：它只看 span 有沒有被壓爛、而且只守 380ms。
⑥**自訂的留白被砍掉**（第四輪：「有留白，但跟原本不同」）。兩個獨立原因：
(a) rightOffset 上限原本是「可見根數的一半、絕對 60」→ 拉到 35 根被砍成 27。放寬到 **80%**
（畫面上一定還留得下兩成 K 棒）、絕對 200。
(b) ★ 一次 `loadData` 會跑**不只一次** `renderAll`（本機快照秒畫一次、真資料到貨再一次），
而 `_pendingRestoreRange` 是**一次性**的 → 第二次進來時它已是 null、`_savedBarSpacing` 開機時也是
null → 掉到「貼最新 N 根」把剛還原好的位置整個蓋掉。症狀極具誤導性：`options().rightOffset`
**確實是 35**（還原成功了），畫面上卻只剩 3 根。修法＝`_bootViewHold()` 讓這個載入週期內的後續
`renderAll` 一律重套同一組（同樣要保險絲＋使用者一動就放手，否則之後的縮放會被彈回去）。
⚠ 守門員的自訂留白值要**大到踩得到夾限**：原本用 20 根，在 49 根可見時剛好落在舊上限（一半＝25）
以內 → 這個 bug 測不出來；35 才會踩到。

⑤**根本沒有預設留白**（第三輪：「還是一樣右邊無留白」）：LWC 預設 `rightOffset=0` ＝最後一根貼著
價格軸，本專案從來沒設過預設值 —— 既有的 rightOffset 邏輯全都只是「保住使用者自己拉出來的位置」。
修法＝`_defaultRightPad()`：依可見根數 6%（夾在 3~20 根）當**下限**，跟已存值取大。
⚠ 用比例不用固定根數（固定根數放大時變半個畫面、縮小時等於沒有）。
⚠ 守門員判準用**像素**且門檻要以 barSpacing 為基準：`rightOffset=0` 量到的仍有 ~12px（半根 K 棒＋
內距），拿 8px 當門檻舊碼照樣通過＝叫不出狼；要求「比兩根 K 棒還寬」才算數。
⚠ 這項必須在動任何東西**之前**測（後面情境會自己設 rightOffset=20，那之後再問「有沒有留白」是必然成立）。

④**右緣留白被夾掉**（使用者第二輪回報：「歷史沒錯，最新框會右貼」）：存進去的 `scrollPos` 是對的
（實測 20 根），是**還原端的夾限**砍的 —— 還原發生在圖表還沒完成佈局那一刻，`ts.width()` 回 0
→ `_visN` 掉到下限 10 → 上限崩成 5 → 20 根留白被夾成 5＝K 棒幾乎貼著右邊。修法＝`_roCapFor()`：
量不到寬度就**不要夾**（只留絕對上限 60）。⚠ 只驗「縮放」和「有沒有貼最新」看不出來，那時它們都是綠的。
另：`saveLastSymbol` 在還原完成前禁止寫入（`_viewRestoreBusy`）—— `loadData` 在 `renderAll()` 後
**立刻**呼叫它，但 LWC 的 `setVisibleLogicalRange` 要下一幀才生效 → 讀到還原**前**的範圍 →
把錨點當場洗成 null（1.2 秒後 debounce 會補救，所以「偶爾才失靈」）。⚠ 旗標**必須有保險絲**：
錨點落在已載資料外會走 `_pendingAlignRange`/`_restoreByBarCount`，那兩條不呼叫 `_holdAnchorByTime`
→ 沒人解除＝從此再也不存檔（比原 bug 更糟）。
⚠ 判準要比**時間**不可比 logical index；⚠ 還原後要**連續盯 20 秒**（漂移是重整後好幾秒才發生，
只驗剛還原那一刻會是綠的）；⚠ 「資料有沒有長大」要跟**重整後**的起點比（重整後是先拿有界視窗
再補回去，永遠補不「超過」重整前那個數字 → 拿重整前當基準這支永遠不成立）。已植回舊碼證明會失敗。

### 在勝率回應新增圖層後（守門員之十三）
```bash
node scripts/check_wr_cache_layers.js   # 需本機服務跑著；約 1 分鐘
```
`fetchWinRate()` 有兩條路（網路成功／`_wrCache` 命中），兩條都要重繪 FVG/SMC/VWAP/通道…各層。
**少寫一層，那層就留著「上一個標的」的資料** —— 圖上照樣有標記、不報錯，只是位置全錯
（已修過兩次：02b429a 補載完成後漏重繪、ca8ec0f 快取命中分支漏重繪）。本檔上面那條
「兩條路徑都要加」的守則原本**只靠人記**，這支把它變成測得到的。
判準＝A（網路）→B→A（快取）後，每一層的**內容指紋**必須完全相同（只驗「有沒有值」不夠：
漏重繪時那層裝的是 B 的資料，有值但是錯的）。圖層清單**從 winrate.js 原始碼 regex 抽**，
不寫死 → 以後新增圖層自動涵蓋。
⚠ 兩個坑（我都踩了）：①`_last*` 是 **bundle 頂層的 `let`、不在 `window` 上** →
`Object.keys(window)` 一個都找不到，要用 `page.evaluate(字串)`＋`eval(名字)`。
②驗之前**必須先關掉 `_snapPaint`**：本機快照秒畫會把該標的的圖層先畫回來（正確的資料），
把漏重繪蓋掉 → 不關就永遠是綠的。關掉之後才是「第一次造訪／清過瀏覽器資料」的人走的路，
而那正是快取分支唯一負責的情境。

### 動到重播模式／即時更新／背景補載後（守門員之十二）
```bash
node scripts/check_replay_no_future.js   # 需本機服務跑著；約 1 分鐘
```
**回測工具最嚴重的一種錯誤**：重播時若看得到游標之後的 K 棒，你做的每個判斷都是作弊、
結論全部無效 —— 而畫面上完全看不出來（K 棒就是 K 棒，不會標示「這根還沒發生」）。
本檔的鐵則「replay 中任何改圖表的操作必須先檢查 `replayActive`」原本**只靠人記**。
判準在做完「前進 40 根／真拖曳／縮放＋等一輪輪詢」之後才驗（那些非同步路徑才是會踩破的地方）。
★★ 判準一定要問 **`candleSeries.data()`（實際畫在圖上的序列）**，
**不可以拿 `ohlcvData`**（我第一版就是，報出 5488 根假的「未來棒」）：`enterReplay()` 是把
ohlcvData **複製**成 replayData 再畫前 N 根，`ohlcvData` 本來就留著完整資料、它是來源不是畫面；
而且重播中背景補載還會繼續往它加舊棒（實測 1150→2533 根）。
⚠ 這條路有**三層**防護（`enterReplay` 的 `stopRealtime()`／`fetchLatest` 入口／`await` 之後再檢一次），
拿掉任一層都還擋得住 —— 植回舊碼時要三層一起拿掉才叫得出狼（實測就會漏出真實世界當下那根）。

### 動到「本機 → 線上」轉送後（守門員之二十五）
```bash
cd backend && ../.venv312/bin/python scripts/check_relay_mirror.py   # 不需服務跑著、不碰網路
```
`_relay_snapshot`（routes/account.py）是**本機這台的資料覆蓋線上（Railway）**的唯一入口，
壞法全都是靜默的：①設定沒鏡像 → 本機刪掉的線上永遠留著 ②鏡像過頭 → 把這台專屬的版面/字級
推上去弄亂另一台 ③**繪圖被當成設定一起鏡像 → 手機畫的線會被這台清掉**（繪圖弄丟救不回來）
④繪圖與設定分兩次讀-改-寫 → 第二次用的是第一次寫入**之前**拉到的快照，把第一次整個蓋掉
⑤本機是空的時照樣鏡像 → 直接把線上清空。
★ 兩者語意**刻意不同**（2026-09-23 使用者：「如果本機刪除 線上也刪除」）：
  **設定＝鏡像**（本機沒有的 key 從線上刪掉）／**繪圖＝逐標的合併、本機優先**
  （本機畫過的那幾檔以本機為準，所以本機刪一條線、線上那檔也會少；
  本機從沒碰過的標的保持原樣 —— 那可能是別台畫的）。
⚠ 安全閥：本機一個可推的設定都沒有時**什麼都不做** —— 那時分不出「使用者清光了」和
  「本機是空的/壞的」，而鏡像會直接清空線上。
⚠ `_RELAY_SKIP` 要跟 `account.js` 的 `_ACCT_SKIP` + `_PULL_SKIP` 對齊：推上去對方也不會套用的
  （版面、字級、看過什麼）就別推，`_tc` 光一個帳號就 108KB。
已植回三種壞法證明會失敗（沒鏡像／繪圖也鏡像／拿掉安全閥，離開碼皆 1）。

### 動到斐波那契工具後（守門員之二十四）
```bash
node scripts/check_fib_tool.js   # 需本機服務跑著；約 40 秒
```
2026-09-22 使用者：「優化斐波」。實測抓到的：
①★★ **右側 775px 的幽靈命中區**：`drawingDist` 只擋左邊、右邊沒擋 → 線只畫到右端點，
  右側卻一路到畫布邊緣都判定「距離 0」。實測斐波畫在 x 296~416，x=596/834/1072/1167 全都抓得到
  → 點畫面右邊三分之二的任何地方都會選到它，**畫面上零跡象**。
  ★ 同 memory `project_crosshair-blank-vline`：**修邊界外的行為先問「另一邊呢」**。
②價格小數位照**價格級距**猜（`p<1 → toFixed(4)`）→ 0.00001234 顯示成 **0.0000**，小幣標籤整排是 0。
  memory `project_price-decimals-from-data` 記過這個坑、當時修了三支，斐波這支是**漏網的第四支**。
  改走全站 `_fmtPx`（問資料）。★ 之後再看到「自己寫一個 toFixed 決定小數位」就是同一個坑。
③層級清單寫死在**兩處**（`drawOne` 的 `_fibLevels`／`drawingDist`）→ 收斂成唯一的 `FIB_LEVELS`。
  漏改命中判定那份＝那條線**畫得出來卻摸不到**，完全不報錯（已植回證明守門員抓得到）。
④`drawPreview` 補上 fib 分支（原本 fib 掉到「一般線段」分支 → 畫第二點時只看得到一條普通虛線）。
⑤**延伸位加了又移除**（同日第三輪，使用者：「斐波那契延伸刪除」）→ 目前**只有 7 條回調位**。
  中間還出過一次「畫反邊」（「斐波那契延伸反了」）：層級原本只存一個 `lvl`，再用
  `p1 + (p2-p1) * (1 - lvl)` 換算 —— 那個 `1 - lvl` 讓 lvl>1 落在**起點外側**（走勢來的方向），
  而延伸位是**目標價**、該在終點外側。
  ★ 功能雖然移除了，**結構上的修法留著**：層級改存 `frac`（佔整段走勢的幾成：0＝起點／
  1＝終點／>1＝終點外）＋ `label`（標籤印的百分比）。**讓「容易搞反的換算」消失，
  比在換算上加註解有效** —— 要再加延伸位就新增一筆 `frac>1`，三處都讀同一份、不必也不可以另開表。
⚠⚠ 判準裡 **`Infinity` 經 `page.evaluate` 會變成 `null`**，而 `null < 12` 是 **true**
  → 直接比大小會把「摸不到」讀成「摸得到」＝判準整個反了（我第一版就是，修好了還報失敗）。
  一律先把 null 正規化成 Infinity。
⚠ 判準一律問**產品自己的函式**（`drawingDist`／`_fibFmt`／`FIB_LEVELS`），不在測試裡複製公式。

### 動到配色儲存／帳號快照同步後（守門員之二十六）
```bash
node scripts/check_palette_sync.js   # 需本機服務跑著；約 40 秒
```
2026-09-23 使用者：「我的 qwer 線上 railway 一直跳回紅色背景主背景」「而且我改完配色到黑色，下次還是跳紅色」。
唯讀核對他的快照：`chartColors.chartBg = #000000`（電腦那份）、`chartColors_m.chartBg = #eb4747`（手機那份）。
兩個環節串起來才會「改了又變回去」——
①**兩份色盤**：舊 `savePrefs/loadPrefs` 用 `_prefKey()` 依 `isMobileUI()` 決定讀寫 `chartColors` 還是
  `chartColors_m`，而 **iPad 一律手機款** → 永遠讀到紅的那份。已合一（只留不帶 `_m` 的三個 key，
  `loadPrefs` 沒有電腦那份時拿 `_m` 當起點再刪掉）。
②★★ **電腦會把紅的復活**：`_acctSnapshot()` 推的是**整包 localStorage**。電腦上那顆
  `chartColors_m`（早年從手機同步下來的）電腦自己永遠不會改它，卻每次 flush 都原封不動再推一次
  → iPad 改成黑、下次開機下行又把紅的蓋回來，**畫面零跡象**。
  ★★ 通則：**整包快照同步時，任何一台留著的死 key 都會被它一直復活。**
  廢棄一個 key 必須同時做三件事：**本機刪**、**列入 `_PULL_SKIP`**、**讀取端改讀新 key** ——
  少做第二件就永遠收斂不了（本機每次開機都刪，雲端每次開機都寫回來）。
③**切到背景的推送必須用 `navigator.sendBeacon`**：iOS 把 app 切到背景會**殺掉進行中的 `fetch`**
  → `_acctFlush()` 送不出去；而下次開啟是全新開機、`_acctSeenTs` 是 0 →
  下行**一定會套用雲端那份** → 剛改的顏色被雲端舊值蓋回去，同樣是「下次又變回來」。
  `_acctBeaconFlush()` 成功就清掉 debounce 計時器；送不出去（>64KB）才退回 fetch。
  ⚠ iOS Safari 常常只發 **`pagehide`**、不發 `visibilitychange`（切 app／鎖螢幕／關分頁）→ 兩個都要掛。
⚠ ④ 沒辦法在 headless 裡重現「iOS 殺掉 fetch」（桌面 Chrome 的 fetch 會正常完成）→ 判準落在
  **送出的機制**（有沒有呼叫 sendBeacon）。這是刻意取捨：差別本來就只在背景化時會不會被殺。
⚠ 線上殘留的死 `chartColors_m` 會由本機的 `_relay_snapshot` **鏡像**自動刪掉（本機沒有的設定鍵，上游也刪），
  不必手動去動真實帳號。
已植回四項舊行為證明會失敗（①②③④a 全紅、離開碼 1）。
⚠ 回傳碼 2＝進不了場／假帳號建不起來（測試不成立），不是通過。⚠ 一律用假帳號名 `__gk_palette_test__`。

### 動到背景往回補載、或深度歷史本機快取後（守門員之二十九）
```bash
node scripts/check_hist_cache.js   # 需本機服務跑著；約 90 秒
```
2026-09-24 使用者：「如果一個使用者常常打開只看 a,b 等標的，就設計成下載歷史資料在該使用者的
裝置，這樣歷史 api 不用另外打」。實測切到 5m 時往回補載那一發是 **140.2 KB**，而那些是
**永遠不會再變的歷史 K 棒** —— 每次開同一個標的都重抓。→ `render.js` `_histFetchJson`
（IndexedDB `ahh_hist`）。實測同一個(標的|時框)第二次載入 **235.2 → 45.8 KB（−81%）**。
★ 刻意做成「**擋在 fetch 前面的一層**」，不是去改合併邏輯：呼叫端拿到的 JSON 形狀完全相同 →
  `_bgLoadGen` 世代守衛、接縫檢查、修剪遮罩、`replayActive` 全部原封不動。那條路是本檔
  記錄過最多事故的地方（往舊滑跳動、heap 1GB、單幀 1127ms），能不碰就不碰。
⚠⚠ **這裡有兩種時間基準，混用會靜默削掉歷史**（我第一版就踩了）：
  ①「涵蓋範圍」比較全部用 `toTime()`＝**圖表時間（+8 小時）**；`body.start/end` 本來就是
    `toIso(圖表時間)` 產生的，`toTime()` 正好轉回同一個基準。
  ②「這段定案了沒」要跟 `Date.now()` 比，那是**真實 UTC**。
  混用的實測症狀：同一區間網路回 4032 根、快取只回 **3457** 根，最舊那端少 8 小時，
  而畫面上完全看不出來（沒破洞、沒錯誤，只是歷史少一截）。
⚠ 命中時**只切比 end 新的那一端，不可以在 start 那端截斷**：呼叫端本來就會再
  `filter(b => toTime(b.time) < existingEarliest)`。原則＝**快取永遠不可以比網路少**。
⚠ 只快取 `end` 比現在早 6 小時以上的區間（`_HIST_SAFE_MS`）：否則會把還在變動的最近幾根
  固化進本機，變成「K 棒不一樣、要清快取才好」——比沒有快取更糟。`window._histClear()` 可手動清。
★★ 判準一定要同時驗**根數**與**最舊那根的時間**：植回那個 bug 時「流量省 43%」**照樣是綠的**，
  只有③④才叫得出狼（已植回證明：③④⑤ 全紅、離開碼 1）。
⚠ 要用**同一個瀏覽器情境**連跑兩次（IndexedDB 才留得住），不可用 `--incognito` 開兩個。
⚠ 回傳碼 2＝進不了場／切不到 5m／第一次就沒觸發補載（測試不成立），不是通過。

### 快照預抓的優先序：正在看的 → 自選 → 常看的（2026-09-24）
`winrate.js` 的背景預抓原本寫死 `["BTC/USDT","ETH/USDT","XAUT/USDT"]` 且 `market !== "crypto"`
直接 return → **台股／美股／港股整個被排除在外**，而且不管使用者實際在看什麼。現在是：
① 你**正在看**的標的的其他常用時框（切時框最需要即時回饋）
② **自選**裡還沒有快照的（各抓 1 個時框，最多前 8 檔）—— 使用者：「加入自選的必要做優先歷史載入下載」
③ 停留分數前 3 名（`symUse`）
⚠ **請求「速率」完全沒變**：照樣 idle 才抓、每個 tick（6 秒）只抓一個、5 分鐘內不重抓、
  已有新鮮快照就跳過。變的只是排隊順序與涵蓋範圍。
⚠ ②③ 只抓**一個時框**：整組 5 個時框只對「正在看的那一檔」做，否則 20 檔自選會排出上百個請求。
⚠ 計分用**停留時間**不用「開啟次數」：翻找標的時會連續點開十幾檔，用次數會把隨手點過的算成常用。
  直接在既有的 6 秒 tick 裡累計＝零額外掛勾、零熱路徑成本；分數每次記分全體 ×0.995（約 14 分鐘半衰）。
⚠ `symUse` 在 **`_ACCT_SKIP`**（裝置本地、不上雲端）：手機跟電腦在看的東西本來就不同，
  而且它高頻寫入，跟著同步會把 2.5 秒的 debounce 一直重設（`_tc` 當年就是踩這個）。
⚠⚠ **勝率 payload 很大，預抓要看「用過沒」再決定**（2026-09-24 量出來的，我自己上一版踩到）：
  單筆實測 **4h 222KB／1h 123KB／15m 108KB／5m 101KB** —— 把正在看那一檔的 5 個時框全預抓要 **581KB**，
  加上自選那批整輪 **831KB**，跟整個冷載（624KB）同一個量級。以前只有 3 檔寫死的加密會觸發所以沒人在意，
  改成「你常看的標的」之後**變成人人有份**。→ 兩道收斂：
  ①**自選／常看清單只抓 K 棒**（使用者要的是「歷史資料存在裝置」，不是勝率）——
    `_snapPaint` 已改成容許「只有 K 棒、沒有勝率」的快照，照樣秒畫 K 棒，標記層讓它走網路。
    ⚠ 沒有 wr 時**整段圖層渲染要跳過**，不可以拿 undefined 去餵 `_render*`（那會把既有圖層清掉）。
  ②**只對「這台裝置真的用過的時框」（`tfUse` 前 2 名）連勝率一起抓**，其餘只抓 K 棒。
    ★ 用「真的切過去看」當訊號，不是猜「相鄰時框」—— 猜的話對只看日線的人永遠是錯的。
  實測 **831 → 158 KB（−81%）**：只看日線的人幾乎不必付，常在 1h/4h 之間切的人照樣秒開。
⚠ 加了**省流量閘門**（`navigator.connection.saveData` 或 2g/3g 就不預抓）：以前只有 3 檔加密會
  觸發，現在任何常看的標的都會，一檔約 5 個時框 ×（K棒+勝率）≈ 275KB —— 行動網路上那不該由我們替
  使用者決定。⚠ LRU 由 12 放大到 30（要容納自選 8 檔＋常看 3 檔，留 12 會互相踢掉＝白抓）。

### 台股報價列：`display` 與 `symbol` 完全相同（2026-09-24）
台股 2700 檔**每一列**的 `display` 都等於 `symbol`（實測 2700/2700），等於把同一個字串送兩遍。
`?nd=1` → 後端省略它，實測整包 gzip **71.2 → 61.6 KB（−13.6%）**，而台股清單是全站最大的一份 JSON。
⚠ **必須由前端帶 `nd=1` 明講，後端預設照舊送**（同 `fd=1` 的教訓）：舊分頁少了 display 會在
  `ticker.js` 的 `t.display.toLowerCase()` **當場爆**。
⚠ 補回來的地方只有**唯一的合併點** `_tkFill` 一處（且必須排在 spot 推導之前，它讀的就是 display）
  → 其餘二十幾個消費者一行都不必改。整包路徑是「先 _tkFill 再建合併鍵」，所以對 `_id()` 安全。
⚠ 只有「display 與 symbol 都在、而且相同」才省：差量列的合併鍵可能只帶 display 不帶 symbol
  （見 `live_data.get_delta`），那種不可以誤砍。對加密是自動 no-op（`BTCUSDT` vs `BTC/USDT.P`）。

### 與其補一個機制產生的縫，不如讓那個縫不要產生（2026-09-24）
面板之間的背景格線會斷，是因為 `.pane-divider` 佔了 7px 高。我先做了一版
「在分隔線裡用 canvas 補畫格線」（取樣圖表畫布找格線 x，再換算成時間、每幀映回 x）——
使用者：「這樣的接法很爛，我縮小就壞了，而且跟不上縮放速度」。**他是對的**：
任何「自己重畫一份去對齊 LWC 那份」的做法都得追上它每一幀的重繪，
取樣有成本、節流就跟不上、不節流就卡頓 —— 本質上在追一個追不到的目標。
→ 正解是把分隔線縮到 **1px**（就是那條分界線本身），相鄰面板的格線自然接上，
  那是 LWC 原生每幀畫的，永遠同步、零維護；拖曳命中區用 `::after` 往上下各撐 4px（不佔版面）。
實測四個面板的格線 x 完全相同（16/100/196/280/382/466/562/646）。

### 改 CSS 前先確認「誰真的生效」（2026-09-24）
「主背景透明度 0 變白」的修法要讓 body 這層地板不透明。我改了 `html, body { background: ... }`，
量到 body **仍然是 rgba(...,0.5)** —— 因為 `html:not(.perf-mode) body`（特異性更高）也設了 background，
結尾是 `var(--bg)`。用 CDP 的 `CSS.getMatchedStylesForNode` 把「實際命中的規則」列出來才看到真正的贏家
（共三條：基本、非極簡、封面頁，全都要改）。
⚠ 也不可以用 inline style 做「地板」這種要穩定的事：`colors.js` 有一行 `body.style.background = ""`
  會在開機流程清掉它 → **選色當下對、刷新後又變回半透明**（實測取樣亮度 67 → 91）。宣告式寫在 CSS 才免疫。

### 主圖區的顏色要均勻 → 底墊必須在天氣層之上（2026-09-24）
使用者：「主圖調透明度上下顏色不一」「但透明度 100% 怎麼就沒差」。
100% 不透明時什麼都透不上來所以均勻；一降低透明度，**天氣天空的垂直漸層**（上亮下暗）就顯出來
→ 主圖上下不同色、副圖又是另一個色。
→ `#chartUnderlay` 由 `z-index:0` 改成 **1**（蓋住 weatherStage）→ 圖表區後面是一片平的純色。
實測四種不透明度下，主圖上/中/下三點像素**完全相同**。
⚠ 代價：主圖後方看不到天氣場景（天氣仍在上方列/標的列/行情列）。要換回來只有那一個數字。

### 動到 CSS 的圓角／字級／間距後（守門員之三十）
```bash
cd backend && ../.venv312/bin/python scripts/check_design_tokens.py   # 不需服務跑著，約 2 秒
```
2026-09-24 使用者：「整體配置想要跟 apple 美學一樣」。稽核發現問題不在配色，在**沒有 token 系統**：
圓角 **28 種**散在 217 處、字級 **24 種**（含十種半像素 8.5/9.5/9.8/10.5/11.5/12.5/13.9/15.5/16.2/18.5px）、
間距 1~13px 奇偶混用。「每個元件各自為政」正是一眼看得出不夠精緻的主因。
→ 收斂成 `--r-xs/sm/md/lg/pill`、`--t-2xs`…`--t-3xl`、`--e-1`…`--e-4`、間距偶數刻度。
⚠ 字級映射用**就近取值、平手往小**：往小只會多出空白，往大會擠爆上方列（那排按鈕的寬度是算出來的）。
⚠ 刻意**不動**的：橘色光暈 17 處與 inset 凹陷質感 22 處（那是使用者要保留的手繪暖感，不是層次）、
  19 個正圓、1px 髮絲線、>32px 大留白。多層組合的陰影也不動（那是刻意的合成）。
★★ **2026-09-26 第二批（使用者：「持續對整體介面 apple 美學化」「需要你多上網查相關資料並整合」）**
　查了現行 HIG／Liquid Glass 規範後，同一套方法論再往下挖三層：
　①**材質**：`backdrop-filter` 散了 13 種模糊值＋4 種飽和度 → 收斂成 Apple 的五級
　　（`--mat-ultra-thin/thin/regular/thick/ultra-thick`，40 處）。**最厚維持 24px**、不讓 GPU 負擔上升。
　②**動態曲線**：7 條只出現 1~2 次的一次性 cubic-bezier ＋ 同一條彈跳曲線抄 16 遍
　　→ `--ease-std/out/spring` 三條（27 處）。裸 `ease` 那 51 處刻意不動（合法語意值、改動大而視覺差異極小）。
　③**字距（tracking）**：Apple 的 SF 是**字距隨字級自動變**，這是它看起來「對」的核心機制。
　　實測表（pt≒px）：`10 +0.12　11 +0.06　12 0　13 −0.08　15 −0.24　17 −0.43 ｜ 20 起換 SF Display → +0.38　24 +0.36`
　　—— 兩段**不連續**（小字要加空氣、13~17 逐漸收緊、20pt 換 Display 後回正）。
　　→ 每個 `--t-*` 配一個 `--tr-*`，**234 處** `font-size: var(--t-*)` 旁邊成對寫上字距。
　⚠⚠ 原本有兩處「Apple 輕量打磨」對所有控制項打 `letter-spacing: -0.01em !important` ——
　　那是**不分字級一律收緊**，以 Apple 自己的表來看方向都可能是錯的（11px 應為 +0.06，被收成 −0.11）。已移除。
　⚠ 我在替換時把 token 定義自己也換掉了 → `--ease-std: var(--ease-std)` 循環定義、整組**安靜失效**
　　（transition 全部退回預設 `ease`，畫面幾乎看不出來）。**做字面值→token 的批次替換時，
　　一定要把定義那幾行排除在外，並在替換後讀一次 computed style 確認解析得出來。**
　④**同心圓角**（Apple：內圓角 = 外圓角 − 內距）：稽核全 DOM，符合 16 組、偏離 13 組；
　　真違規是浮動繪圖島裡的三顆（4/8px 在 18px 島＋6px 內距裡 → 應 12px）。修完 20 組符合，
　　剩 9 組全是刻意的正圓（圓點、圓形關閉鈕）——**稽核腳本要排除正圓**，否則永遠有誤報。
　⑤**觸控目標 44×44**（HIG 硬性下限）：實測手機 20 個不足 → 8 個。
　　⚠ `.tb-icon-btn`／`.tf-btn` 有 `overflow:hidden`（漣漪要裁在鈕內）→ **用 ::after 撐命中區會被裁掉**，
　　　那條路走不通，只能真的做到 44（它們在可橫捲的列裡，變寬只是捲得長一點）。
　　⚠ 其餘沒有 overflow 的（星號／圖表類型／上下顛倒）才用 ::after：視覺不變、命中區 44。
　　⚠⚠ `#marketPill` 看起來 68×28 不合格，其實是 `pointer-events:none` 的**裝飾**膠囊
　　　（真正接點擊的是父層 378×44）—— 我對它加 min-height 完全沒有用，只是把外觀拉高了。
　　　★ 通則：**先確認那個元素真的收得到點擊，再談它夠不夠大。**
　　⚠ 判準要同時驗「擴大後接得到」與「**沒有搶走鄰居的中心點**」：相鄰只差 2px 的地方硬撐命中區，
　　　重疊區誰接到取決於 DOM 順序，那比小一點更糟。
　⑥**彈出面板與面板內按鈕**（使用者：「額外彈出的小面板或按鈕配置也要美化」）：
　　・圓角：那張「圓角統一」清單**漏了系統外觀與提示鈴**兩個面板（停在 8px，同類都是 18px）→ 補上。
　　・材質：全站只有繪圖島是材質，其餘彈出層都是不透明色塊 → 六個面板統一吃 `--mat-thick`
　　　＋底色 88%＋上緣 1px 內高光（specular highlight），與繪圖島同一個作法。
　　　降低透明度時自動全部轉不透明（毛玻璃元素 14→20 個，開關一開全部歸 0，實測過）。
　　・**按鈕高度由度量決定、不由內容決定**：系統外觀那四顆全寬鈕 CSS 完全相同，高度卻是
　　　26/28/28/27 —— 差別來自內容（emoji 的行框比純文字高、SVG 又不一樣）。
　　　→ `min-height` + `line-height:1` + flex 置中 + gap:6px。特效面板 31/32 → 32。
　　⚠ 我差點又用眼睛判斷：指標設定面板「看起來」欄位沒對齊，**量完是 12 列全部 26px、±11px 完全一致**。
　　　★ 通則：版面問題一律先量（列高、內容左右緣、按鈕高度分佈），不要憑截圖印象動手。
　　⚠ `.btn-reset` 改 flex 之後面板從 249px 縮到 200px（按鈕的固有寬度本來在撐面板）——
　　　那是好事（少了死空間），但**改 display 會連帶改變父層寬度**，改完要看一眼。

　⑦**按鈕設計**（使用者：「按鈕設計也要優化」）：
　　⚠⚠ **`:hover` 在觸控裝置上會黏住** —— 全站 119 條 hover 規則只有 1 條有 `hover: hover` 保護。
　　　實測手機輕點上方列圖示鈕後，它**卡在 `scale(1.12)`＋背景變色＋橘色暈光**，要點別處才消失
　　　（看起來像「這顆被選取了」）。→ 119 條全部包進 `@media (hover: hover)`。
　　　⚠ 唯一保留不包的是 `.dt-edge-trigger:hover ~ .draw-toolbar`：那是「hover 顯示繪圖島」，
　　　　包起來在觸控裝置上就永遠打不開了。判準＝找 `:hover` 後面還接別的元素的選擇器（9 條，
　　　　其餘 8 條都是自身內部的子元素樣式，可以包）。
　　　驗證：桌機 hover 仍 1.08 放大、移開復原、繪圖島仍會展開；觸控輕點後 transform=none。
　　・**互動回饋收斂成兩個 token**：按下 9 種縮放（.88/.90/.92/.93/.94/.96/.985）→ `--press: .96`（17 條）；
　　　hover 6 種（1.05~1.2）→ `--hover-pop: 1.08`（9 條）。
　　　⚠ 刻意保留：帶 rotate 的角色動作（星星、小熊、幣種 logo）＝品牌個性；**列**（行情列、
　　　　搜尋結果列）的 .985 —— 列不是按鈕，回饋本來就該更輕。
　　・圓角按**控制項尺寸級距**對齊：34px 級 12px、28~32px 級 8px；`.tb-ct-btn`(26px) 停在 4px → 8px。
　　⚠ 批次改 CSS 的掃描器要處理 `@media` 巢狀：我把 hover 規則包進 media 之後，第一版的替換
　　　只改到 1 條（掃描器把 `@media …{` 當成選擇器、抓到的 `}` 是內層規則的）。檔案沒壞（原樣重組），
　　　但**「只改到 1 條」這種數字不對勁的結果要當成錯誤看，不能當成「剛好只有一條」**。

　⑩**合約行情面板**（使用者：「合約行情那裡做 apple 美學優化」）：
　　・**分隔線內縮對齊文字**（Apple 列表最好認的細節）：原本滿版 `border-bottom` 連頭像一起切開，
　　　改成 `::after` 從內容左緣（38px＝內距6＋頭像26＋間距6）起畫；最後一列不畫。
　　・**選中列改成內縮圓角色塊**：原本是「整列滿版底色 ＋ 左邊 2px 直條」兩種語彙疊著。
　　　⚠ 色相維持橘子熊的 `--accent`（我第一版誤用 `--blue`，那是另一個語意）。
　　・**分段控制項同心圓角**：容器與選中片原本都是 8px、內距 2px → 角度一樣＝非同心。
　　　容器提到 12、內部維持 8（12−2=10 與 8 接近，兩個值都在既有刻度上，不為了同心新增第六級）。
　　・**回收水平空間給標的名**：內距 8→6、欄間距 8→6、頭像 30→26、漲跌行間距 4→2。
　　　實測截斷 **30/40 列 → 15/40 列**，名稱欄 67→73px；列高 47→44（仍達 44 觸控下限）。
　　⚠ 剩下的截斷（VELODROME/USDT.P 需 125px）要再拿 ~43px，只能動右欄的「漲跌額」——
　　　那是**刪使用者的資料**，已列 A/B/C 三案給他決定，未擅自動。
　　⚠ 守門員當場抓到我自己：漲跌行間距我寫 `3px`，`check_design_tokens` 報「不在偶數刻度上」→ 改 2px。

　⑨**細節收尾**：
　　・`:disabled` 從 **7 種寫法**（透明度 .3/.5/.6、游標 default/not-allowed 混用）收斂成
　　　`--disabled-opacity: .35` ＋ `cursor: default`（7 條規則）。
　　　⚠ 游標用 `default` 不用 `not-allowed`（那個禁止符號是 Windows/Web 習慣，macOS/iOS 是一般箭頭）；
　　　⚠ 不用 `pointer-events: none`（會連 title 提示一起吃掉，使用者就不知道為什麼不能按）。
　　・鍵盤焦點實測：Tab 過 14 個可聚焦元素，看不見焦點的只剩 `#symbolInput`
　　　（那是使用者明確要求拿掉的橘框，保留）。
　⚠⚠⚠ **這一輪最重要的教訓：機械改寫 CSS 會安靜地改變巢狀層級。**
　　把 119 條 `:hover` 包進 `@media (hover: hover)` 時，我的掃描器沒認出**寫成一行**的
　　`@media (max-width: 1180px) { .x { … } }` → 把「鍵盤焦點」那條規則整個關進手機斷點裡
　　（＝寬螢幕上焦點環失效）。**大括號總數是平衡的、CSS 也解析得過、瀏覽器不報錯** ——
　　只有那條規則的適用範圍變了。
　　★ 驗法：改寫前後各算一次「每條規則的巢狀深度」逐條比對（實測只有 1 條被位移，
　　　修好後 0 條）。這比數大括號有效得多。
　　★ 已加進守門員（`check_design_tokens` ⑥）：`:root` / `button:focus-visible` / `html, body`
　　　這幾條全域規則必須在最外層；已植入違規證明會失敗。順帶抓到第二條受害者
　　　（全域自訂游標規則含 `*:hover`，也被包進去了）。

　⑧**系統輔助開關**：`prefers-reduced-transparency`／`prefers-contrast` 全站原本 **0 處**。
　　⚠ 兩個都**必須做在 JS**：面板背景是 colors.js 的行內 `!important`、色彩 token 被 ui.js 的
　　　`SC_DEFAULTS` 整組覆寫 —— 純 CSS 的 media query 壓不過（實測寫了沒生效）。
　　實測：面板 alpha 0.7→1、毛玻璃元素 14→0；次要文字對比 5.29:1→8.33:1（AA→AAA）。

⚠ 我在這支上踩了兩次自己的 bug：①用「有逗號」判斷多層陰影 —— 但 `rgba(0,0,0,.5)` 本身就有逗號，
  結果 **0 處被收斂**；②用 `\d+px` 抽 blur —— 但 `0 4px 20px` 的第一個 `0` **沒有單位**，只抓到
  兩個數字就判定「不是投影」。解析 CSS 值時，先想清楚「括號」與「無單位 0」。
已植入三種違規（裸 px 圓角／半像素字級／奇數間距）證明會失敗。

### 主圖與副圖「對不齊」的根因是價格軸寬度（2026-09-24）
使用者：「主圖跟副圖的線接不起來」「是指背景的格子線」。
★★ **不是線斷掉，是橫向錯位**：每張圖的價格軸依自己最寬的標籤決定寬度
（實測 main 70 / kdj 64 / rsi 64 / macd **76**）→ 繪圖區寬度不同 → **同一個時間落在不同的 x**
（實測十字線四段 x 是 534/537/537/531，最多差 11.6px）。背景垂直格線同理，各畫各的。
→ `charts.js _syncAxisWidth()`：取四張裡最寬的軸，用 `minimumWidth` 套給全部。
⚠ 量軸寬**不可以用 `timeScale().width()`**：實測它對主圖/KDJ/RSI 一律回 **0**（只有帶可見時間軸的
  MACD 回真值）。要用 `priceScale("right").width()`，四張都回真值。
⚠ 用「取最大」不可寫死數字：換低價幣（0.00001234）軸會變寬，寫死就又錯開。
⚠ 在 `resizeAll` 與 `renderAll` 之後各對齊一次，且要用 rAF（applyOptions 後 LWC 下一幀才重算軸寬）。
★★ **查這類問題要比各段的 `left`，不是比「上下有沒有空隙」** —— 我比了好幾輪 y 方向的空隙，
  一直是 0（全綠），真正錯的是 x。
⚠ 量半透明疊加的東西時，**先把它改成不透明純色再量**：十字線是 `rgba(117,134,150,.65)`，
  疊在面板背景與疊在分隔線（透明）上 composite 出的顏色不同 → 顏色比對只抓得到其中一段。
  我連續量錯三次（挑到隔壁 K 棒欄／閾值把成交量柱算進去／顏色比對只中一段），
  最後把線改成不透明洋紅才量準。

### 格線要貫穿面板之間（2026-09-24）
使用者：「上下的格子線要連起來，但中間一樣要像現在有區隔」。
→ `charts.js _drawGridBridge()`：每條 `.pane-divider` 裡放一張小 canvas 補畫格線；
分隔線那條 1px 水平線畫在它**上面**，「貫穿」與「有區隔」同時成立。
⚠ 格線的 x **從圖表已經畫好的畫布取一列像素找**，不自己依 tickMarkFormatter 或時間推算 ——
  LWC 的刻度演算法一改就會錯開幾 px，而那種錯開極難發現。
⚠ 節流到「停手後 140ms」才重畫：它要 `getImageData`，不可每幀跑。

### 「主背景透明度調到 0 變白」＝ body 這層地板透明了（2026-09-24）
`ui.js applySystemColor` 原本 `document.body.style.background = applied`，而 applied 帶著使用者選的
alpha → 拉到 0 時 **body 整個透明，露出瀏覽器預設的白色畫布**。
植回舊行為證明：取樣亮度 **236（白）**；修正後 **54（暗）**。
→ body/html 一律吃去掉 alpha 的 `--bg-solid`（那個變數本來就存在，當初是為了手機分頁才加的
——「因為透明度導致其他分頁背景有圖表」，**同一類問題的第二次**）。
上方面板仍吃帶 alpha 的 `--bg`，所以「天氣從 topbar 透出來」那個特色完全保留。
⚠ 我一開始修錯地方（去改 `chartBg` 主圖背景）—— **「主背景」是系統外觀、「主圖背景」是 chartBg，
  兩個是不同的色盤**（claude.md 早就寫著「主圖控制主圖，主背景控制上方跟右邊」）。
⚠ 順手修：`_lum()` 的正則只吃 6 位 hex，遇到帶 alpha 的 8 位就 fallback 回 128，
  而判斷是 `lum < 128` → **只要背景有任何透明度，格線配色就會反掉**。

### 價格顯示的小數位「只進不退」（2026-09-24）
使用者：「hype 小數點後三位在主圖上變兩位了」「我要精確數值」。
根因在**資料來源**：實測同一檔 HYPE，binance / pionex / bybit 永續都含 3 位小數，
但 **bybit 現貨**回來的分佈是 `{0:15, 1:205, 2:1544}` —— **一筆 3 位都沒有**，只能推出 2。
Binance 一冷卻、fallback 落到 Bybit 現貨，第三位就在上游被四捨五入掉了。
→ `render.js` 以**標的**為單位記住看過的最高位數，降級來源不讓它把位數拉低。
★ 兩種錯的**代價不對稱**：少一位是丟掉真實資訊（92.698 → 92.70）；多一位只是多一個尾零。

### ⚠ 守門員會因為「結構改了」安靜地變成空洞測試（2026-09-24）
`check_topbar_reachable` 原本用 `tr.children` 列舉按鈕。把圖示包進 `.tb-group` 分組之後，
直接子元素從 14 顆按鈕變成 5 個容器 → **這支照樣全綠，但測到的東西從 14 個掉到 6 個**。
而「容器中心點可達」根本不代表裡面每顆按鈕都可達。
→ 改成掃**可互動的葉節點**（對任何巢狀結構都成立），並加「列舉不到 8 個就回傳碼 2」的數量下限保險。
★ 通則：**任何「列舉一組東西再逐一驗」的守門員，都要有數量下限** —— 否則列舉壞掉時它會變成
  「因為沒東西可測而通過」（同 check_diag_auth 的教訓，那是第二次）。

### 往 static/img 加圖片後（守門員之二十八）
```bash
cd backend && ../.venv312/bin/python scripts/check_img_optimized.py   # 不需服務跑著，約 10 秒
```
2026-09-24：7 張 PNG 合計 143.6 KB，無損重壓後只要 **88.6 KB —— 省 54.9 KB（38%），
解碼後像素逐位元相同**（每張都比對過 RGBA 的 md5）。其中 4 張在冷載關鍵路徑上
（bear / bear-full / icon-192 / cursor-wand），實測冷載 **642.5 → 624.4 KB**。
★ 繪圖工具匯出的 PNG 幾乎都帶著沒用的 chunk 與未最佳化的濾波器，而圖片是**會一直被加進來**的東西
  —— 沒人會記得每次手動壓。這種浪費**完全無聲**：圖片顯示正常、零錯誤，只是每個人都多付頻寬。
判準＝對每張實際跑一次無損重壓，還能再省 >12% 就算沒壓過（且要**確認真的無損**才報）。
⚠ 門檻不可訂太緊會叫狼來了：不同 oxipng 版本差幾個百分點，而沒壓過的匯出檔實測 **35~45%**，
  取 12% 兩者差很遠。已植入「PIL compress_level=1 重存的 bear.png」證明會失敗（報 41%、離開碼 1）。
⚠ 回傳碼 2＝沒裝 `pyoxipng`（測試不成立），不是通過。**它是開發用相依，刻意不進 requirements.txt**
  —— Railway 執行期用不到，塞進去只是讓映像變大。
⚠ **不可以**改成「用 256 色調色盤還能更小就算失敗」：那是有損的（實測 bear.png 24.1→4.6 KB，
  但這幾張是水彩風插畫、漸層會出色帶）。這支只走無損那條路。
★ 同批量過、**確認沒有空間、別再重試**的（省得下次又花一輪）：
  ①**城堡 webp 原檔就相當於 q=85**，同品質重壓 81→81 KB 完全沒省；降到 q=75 省 17 KB
    但 SSIM 掉 3.2 dB（拿 app 第一印象換，不划算）。
  ②**Caveat 字型子集化不值得**：最激進的只留 ASCII（會弄壞有重音的國名）也才省 9.6 KB／19%
    —— 它大是因為草寫外框複雜，不是字多（226 碼位 49 KB，M PLUS 同樣 230 碼位只要 21 KB）。
  ③PNG→WebP 可以再省一半，但 `bear.png` 是 **favicon**、`icon-192.png` 是 **PWA/apple-touch-icon**
    （iOS 那個不吃 webp）→ 換格式的相容性風險換 10 KB，不做。
⚠ `M_PLUS_Rounded_1c-500-*.woff2`（57 KB）是 2026-09-12 從 fonts.css 移除字重後留下的**孤兒檔**，
  沒有任何人會下載它 —— 佔的是 repo 空間不是使用者頻寬，要刪再刪。

### 動到 /api/ohlcv 回應欄位或副圖顯示條件後（守門員之二十七）
```bash
node scripts/check_ohlcv_slim.js   # 需本機服務跑著；約 40 秒
```
2026-09-24 量測：KDJ / RSI / MACD 三個副圖**預設是 `display:none`、0×0**（全新造訪 `collapsedPanes`
是 `{}`＝這是預設、不是使用者收起來的），而只給它們用的 8 個欄位
（rsi_14 / rsi_7 / macd·signal·hist / kdj_k·d·j）佔整包 **brotli 後 26.11 → 12.05 KB＝54%**。
前端早就用 `indicators: !_subchartsHidden()` 告訴後端不要送（`utils.js` buildPayload）——
**但這件事之前沒有任何測試守著**。★ 它壞掉的形狀最毒：圖表完全正常、零錯誤，
只是**每次載入圖表都多下載一倍**，而 `/api/ohlcv` 不只冷載會打，**每次換標的、換時框都打一次**。
⚠⚠ `bb_upper/bb_middle/bb_lower` **不可以**一起砍：布林畫在**主圖**上、實測預設 `visible=true`
（1132 點），跟三個副圖不同類。最容易犯的錯就是「這些也是指標，一起砍吧」→ 主圖少三條線。
⚠ 沒帶 `indicators` 旗標時後端**必須回完整版**（舊分頁／舊版 JS 讀不懂瘦身格式）——
同守門員之十六：**前端宣告能力、後端預設走舊格式**。
已植回三種壞法證明會失敗（後端無視旗標／前端一律要指標／連布林一起砍，離開碼皆 1）。
⚠ 判準取**中間那根**K 棒問欄位：頭幾根的指標還沒算出來是 null，拿第 0 根會誤判。
★ 同批量到、但**查證後確認已經是最佳狀態、別再動**的三項（省得下次又花一輪重查）：
  ①`/api/tickers` 的 `fd=1`＋`since` 欄位級差量正常（實測 40/41 都有帶）
  ②城堡開門圖的暖載時機是**刻意的取捨**，`main.js` 註解寫著「別再花時間調優先權」（慢 4G 三種都量過）
  ③Caveat 字型不是死重：城門上的國名（`.landing-country`）在用。
★ 冷載基準（本機、全新 profile、2026-09-24）：**642.5 KB / 45 個真的打到網路的請求、271 ms 看到 K 棒**；
  `/api/ohlcv` 熱快取 **3.1 ms**（冷 341 ms）。⚠ 量的時候**不可以關瀏覽器快取**：
  關了以後 favicon 那次會從「磁碟快取」變成真的打網路 → 量出假的「bear.png 抓兩次」
  （同守門員之十九的教訓，我又踩了一次）。incognito 本來就是空快取＝真實的「第一次造訪」。

### 帳號快照的寫入邊界要擋掉「純裝置本地」的 key（2026-09-24）
`_strip_device_local`（routes/account.py）在 `/api/account/sync` 寫入前丟掉 `_tc`／`wxCoords`／
`notifyFeedSeen` —— 它們是純裝置本地、**全後端零讀者**，進了雲端只是死重。
實測帳號 `Abc` 的快照 125.8 KB 裡 `_tc` 就佔 **106.4 KB＝84%**，而 `/api/account/pull` 是**整包回傳**
→ 那 84% 每次開機都被下載、然後被前端 `_PULL_SKIP` 原封丟掉。它是 2026-06-12 留下的化石
（`_tc` 2026-08-05 才被加進 `_ACCT_SKIP`）——現行前端早就不上傳了，但**舊分頁／舊版本隨時可以再送上來**，
而且沒有任何人會清掉它。★ 同「整包快照會讓死 key 永遠復活」：光靠前端不上傳不夠，**邊界要有一道**。
⚠⚠ `watchlist` **不可以**列進去：`routes/notify.py` 會從快照讀它決定要通知哪些標的
（`_coerce((data or {}).get("watchlist"))`）—— 剝掉就是**靜默關掉自選標的的訊號通知**。`notifyPrefs` 同理。
⚠ `tradeKey`／`acctName` 刻意不動：前者是交易口令（碰它等於動認證路徑），後者只有幾十位元組。
⚠ 已存在的化石不會自己消失（那些帳號不再同步）→ 要清得另外跑一次，且那是**真實使用者資料**。

### 動到盈虧比工具（longpos/shortpos）後（守門員之二十三）
```bash
node scripts/check_pos_tool.js   # 需本機服務跑著；約 40 秒
```
2026-09-21 使用者：「盈虧比不好用」。三個結構性缺陷，都**不會報錯**：
①★★ **整個盒子搬不動**：`_drawingHitPart` 取「離哪條線最近」且**沒有距離門檻** →
  色塊內每一點都被 entry/tp/sl 瓜分，**從不回 `"move"`**。實測從色塊中間往下拖 48px：
  只有停利線被拉走，**RR 1:1 → 1:0.31**。想平移卻在拉線，畫面零跡象，只是盈虧比被拖爛了。
  ★ 其他型別本來就是 `_endpointHit(...) || "move"` —— 這裡少了同一層，**修法就是補上門檻**（`_POS_HIT`）。
②建立時第二點的語意會翻轉（點上面＝停利、點下面＝停損），另一邊鏡射成**強制 1:1**。
  現在固定＝**停損**（交易實務先定風險），停利照 `RR_DEFAULT` 放；
  **方向由停損在哪一邊決定、不看按了哪顆工具** —— 按做多卻把停損點在上面，那是一張做空的單，
  照按鈕硬做只會生出「做多但停損在上面」的盒子（風險為負、RR 無意義）。
③盒子上只有 `1 : x.xx` → 加第二行「報酬 +x.x%　風險 −x.x%」（用**百分比**不用點數：跨商品才比得出來）。
★ 三種拖曳各司其職（使用者：「我要能固定上下止盈止損線，能調整中間進場線」）：
  **拖進場線＝只動進場**（上下釘住、RR 跟著變，夾在停損停利之間留 2% 縫避免風險＝0）／
  **拖色塊空白處＝整體搬移**（三條一起走、RR 不變）／拖停利或停損＝只動那條。
  ⚠ 整體搬移原本掛在進場線上，那個需求搬到「色塊空白處」才空得出進場線來調。
⚠ 順手修：`drawPreview` 的做空色塊**兩色是反的**（紅塊畫在獲利側）—— 改成不看方向算絕對範圍就不可能配錯。
④**拖曳中十字線整個凍住**（2026-09-22 使用者：「鼠標按著盈虧比線做調整，十字虛線不會跟著動」）：
  `_onChartMouseMove` 的拖曳分支 `stopPropagation()`，而那個監聽掛在 chartEl 的 **capture 階段**
  → 事件根本到不了裡面的 LWC 畫布。拖盈虧比時最明顯：正在調價格，卻看不到游標對應的價。
  ★ 擋 pan 的是 **mousedown** 那次 stopPropagation —— LWC 沒收到 mousedown 就不會進入平移狀態，
  mousemove 放行只會更新十字線。這條對**所有**繪圖拖曳都成立，不只盈虧比。
  ⚠ 判準要**兩件事一起驗**（十字線會動 ＋ 可見範圍不變）：只驗前者的話，把 mousedown 的攔截
  一起拿掉照樣通過、但圖會被平移；只驗後者則原本的 bug 本來就是通過的。
⑤**縮放 K 棒時標籤會跑掉**（2026-09-22 使用者：「盈虧比有新問題是 我縮放Ｋ棒他會跑掉」）：
  `_drawRRLabel` 舊版用 `rx - ex > bw + 10` 決定「盒內置中／盒外右側」——**盒寬會隨縮放變**
  （實測 24~161px），而 ③ 的第二行把標籤撐到 120px 之後，日常縮放就一直跨過那個門檻 →
  標籤相對錨點在 **96/21/50/29px** 之間亂跳。修＝`_rrLabelX(ex, bw, W)` 只吃錨點與畫布寬，
  一律貼著盒子左緣（固定 6px），只有整塊要掉出右緣才往左收。
  ★ 通則：**畫在圖上的東西，位置只能依賴「錨定在資料上的量」**（時間→x、價格→y、畫布邊界），
  不能依賴任何隨縮放變動的像素尺寸 —— 否則縮放時它就會自己跑。
  ⚠⚠ 判準要讀 `window._rrLblX`＝**那一幀真的畫上去的 x**。問 `_rrLabelX` 或在測試裡複製一份
  公式，都只是「自己測自己」：有人把盒寬依賴寫回 `_drawRRLabel` 內部時會照樣通過（我第一版就是）。
⚠ 判準要問**位移量與 RR**，不是「有沒有動」：拉單線時畫面上也「有東西在動」，看不出來。
⚠ capture 階段的 `stopPropagation` 會**連自家圖表庫一起切斷** —— 想擋某個互動時，先問
  「擋在哪一個事件才夠」：擋 mousedown 就夠的事，別擴大到整串 mousemove。
⚠ 一律用假帳號名（`__gk_rr_test__`）；回傳碼 2＝進不了場（測試不成立），不是通過。
已植回舊命中判定證明會失敗（4 項紅、**離開碼 1**；現行離開碼 0）。
⚠ 量離開碼別接 `| tail`：`$?` 會變成 tail 的，永遠是 0 ＝ 以為自己有守門員（我就這樣誤讀過一輪）。

### 動到繪圖圖層 A/B/C 後（守門員之十一）
```bash
node scripts/check_draw_layers.js   # 需本機服務跑著
```
繪圖是**使用者自己畫的資料，弄丟救不回來**（2026-08-12 我用測試腳本清掉過使用者 118 個繪圖）。
圖層動到的正好是兩條靜默失敗的路徑：①存檔時若把隱藏層濾掉 → 重新整理後那層**永遠消失**、
零提示；②命中判定若沒濾掉隱藏層 → 使用者會拖到／刪到**看不見的線**。
判準＝隱藏一層後記憶體與 localStorage 的繪圖數都不變／重新整理後三層都在且隱藏狀態記得住／
`findNearest` 對顯示層命中自己、對隱藏層必須沒命中。已植回舊碼證明會失敗。
★★ **測試一律用假帳號名 `__gk_draw_test__`**：設成真實帳號名的話 account.js 會把整包
localStorage 快照同步進後端 → 測試就會寫壞真實資料。假名字查無帳號 → `/api/account/sync`
回 404 什麼都不寫（**測試過程會看到那個 404，那是正確行為**）。
⚠ 造測試繪圖時端點時間要用**真的存在的 K 棒時間**：LWC 的 `timeToCoordinate` 對不存在的時間
回 null（我第一版用 `t±7200` 秒，日線上不落在任何一根 → 命中判定那段整個沒測到卻「通過」）。

### 動到十字線／crosshair 相關後（守門員之十）
```bash
node scripts/check_crosshair_blank.js    # 需本機服務跑著
```
鉛直線**不是 LWC 原生的**（原生已關掉），是 `charts.js` 自繪的四個 `.pane-vline`，靠
`subscribeCrosshairMove` 的 `param.time` 定位 → **空白區沒有對應時間**就會整批隱藏，
但橫線／價格標籤走另一條路徑還在 ＝ 使用者看到「十字線只剩一半」（2026-08-13 回報）。
⚠ **空白區有兩邊、程式路徑不同**：右側（最後一根之後）一直有 `positionLinesByX`，左側漏了；
左側空白很常見（1M/1d/8h 資料少、或縮到最小看見全部時）。修邊界外的行為**先問「另一邊呢」**。
判準＝左空白/K棒區/右空白三處線都在、在空白區內**會跟著游標動**、離開圖表要收掉。

倉庫檔（`backend/data/klines5m/*.pkl.gz`）是**版控、會隨 git 上 Railway** 的。暖機/回填只要缺一塊，洞就被固化進檔案，之後所有讀倉庫的請求都拿到有洞的資料——**不報錯、K 線只是少一段**。2026-07-30 實測抓到 BTC 5m 缺 434 根、BTC/ETH 4h 各缺 10 個月，全都已上線才被深滑 E2E 發現。`--fix` 會分段補抓（**必須分段**：跨永續上線日的長區間，fapi 回「非空但只有上線後那截」→ fallback 不觸發 → 前段被靜默丟掉）。詳見 memory `project_klines-store-holes`。

## 📚 詳細文件（做相關工作時再讀，避免每輪載入吃 context）
> 架構細節已從本檔拆到 `docs/`，需要時用下方路徑讀取對應檔案即可。
- **後端**：環境變數、資料夾結構、資料源、即時行情疊加（台股分鐘K）、天氣資料源、背景載入策略、Pionex 限流、已知問題 → [docs/backend.md](docs/backend.md)
- **前端**：JS 模組表、視覺特效、音效、極簡模式（perf-mode）完整說明、版面配置、圖片資源、星號按鈕、標記視窗化 → [docs/frontend.md](docs/frontend.md)
- **策略標記計算 `/api/crt_winrate`**：輸出哪些鍵、誰在用、vw 階梯／跳過圖層／差量／快取、守門員；勝率統計與 S1~S12／SS 訊號已移除（考古清單在文末） → [docs/crt-winrate.md](docs/crt-winrate.md)
- **FVG 策略定版規格（v2.3，參數已鎖定）**：止損/止盈檔位、雙槽多空、多幣組合、止盈先到撤殘單 → [docs/fvg-strategy.md](docs/fvg-strategy.md)
  - ⚠ 主圖方向多空/破多空標記（`crt.py` `_calc_crt_winrate` 的 `_pseq` proto 缺口）**2026-07-10 拿掉 g+1「沒填回」檢查** → proto 純「g 收盤站上前根高/破前根低」即定案、**不再被下一根收盤回頭撤掉（非 repaint）**；代價破多空標記約 2x。**未收盤最後一根**另出「暫定」標記（半透明+空心+?，`_prov_proto`，收盤才轉正式、會 repaint、使用者已同意）。auto-trade 進場 `_fvg_sigs` 是另一套、不受這些影響。
- **3D 天氣背景實作規格**：Canvas 2D 粒子＋CSS 3D 分層、Phase 進度與實作差異 → [docs/weather-3d-spec.md](docs/weather-3d-spec.md)
- **自動交易引擎**（Binance USDⓈ-M 永續，testnet 預設、逐帳號自有金鑰）：`routes/trade.py`＝下單/對帳/生命週期，`notify_monitor.py`＝背景偵測訊號→下單。訊號源子設定 `{ss, fvg}`：ss=SR/SMC 反轉、fvg=失衡缺口。核心 `execute_signal_trade`／`_exec_signal_for_account`（市價）、`place_fvg_limit_ladder`（限價）＋各自 `reconcile_*`。
  - ⏸ **2026-09-17 起自動交易暫停**：環境變數 `AUTOTRADE_PAUSED` 預設 `1` → 所有**新進場**入口（`_autotrade_blocked()`）直接略過；既有倉位/掛單的 `reconcile_*` **照跑**（停掉會讓已開的倉沒人管）。恢復＝Railway 設 `AUTOTRADE_PAUSED=0`。⚠ 暫停**不會**撤掉交易所上已掛的限價單。
  - 🗑 **SR+SMC 教練（coach）已於 2026-09-17 整個移除**（UI／`/api/smc_coach`／`/api/coach_scan`／掃描推播／`place_coach_limit`／`utils/smc.py`）。**刻意保留** `reconcile_coach_pending(_all)`：管理移除前教練開過、還掛著或還持有的單；`_clean_coach` 一律存成 `on=False`。`window._coachVWAP` 只是 VWAP 沿用的舊變數名，不是教練。`crt.py` 的 `smc_*`／`channel` 計算也已刪（2026-09-17，全站零讀者；勝率計算快 25~42%）——⚠ sweepBoost 讀的是 **FVG 缺口自己的 `sweep` 欄位**，跟 `smc_sweep` 無關，別被名字騙了（我就寫錯過一次）。

## ⚠️ 關鍵鐵則（違反會造成 bug，務必遵守）

### 時間戳
- 所有圖表時間戳 **+8 小時**（Taiwan Time），由 `toTime()` 處理。
- **後端傳前端的時間戳一律用 `.isoformat()`，禁止 `str(pd.Timestamp)`**：空格格式會讓 `toTime()` 產出 NaN → 餵 `setMarkers()` 後 Lightweight Charts 內部損壞 → **十字線鉛垂線全面斷裂**。已封裝於 `_ts(row)`。
- 台股 yfinance（`fetch_tw_intraday_yf`）：naive timestamp 先 `tz_localize("Asia/Taipei")` 再 `tz_convert("UTC")`，且 localize 前必須 `if df.index.tz is None`（否則 double-localize／小時線位移 +8h）。

### 前端 bundle 打包
- bundle 串接順序＝ `main.py` `_build_js_bundle()` 的 `names` 串列。**新增 bundle 檔務必同步加入 `names`**，否則不會被打包。
- 拆 bundle 檔：依行邊界切、在 `names` 同位置插入 → minify 後位元組相同（零行為風險），拆檔走這條路。
- 動態載入檔（`effects.js`/`weather.js`，不在 bundle）：拆/改後要更新 `main.js` 的 `_loadFx` 與 `main.py` 的 `_asset_ver`（mtime 版號，否則 `/static` 長快取吃到舊檔）。

### 前端關鍵庫一律自架、勿用外部 CDN（2026-07-10 重大教訓）
- **圖表庫 LightweightCharts、字型（M PLUS Rounded 1c／Caveat）已自架於 `frontend/static/vendor/`**，`index.html` 從同源載入（`/static/vendor/...?v={{ ver }}`）。**不要改回 unpkg / Google Fonts CDN**。
- 為什麼：CDN 對某些使用者網路不可達（iPad、部分 Windows／公司網／ISP）→ `LightweightCharts` undefined → bundle 早期 `makeBaseOpts` 拋錯 → 整包後續（建圖表/城門/登入）全不執行 → 整個 app「進不去」。開發者本機因 CDN 已快取而永遠正常、極難自測發現。
- 診斷「某裝置進不去但我這正常」→ 首疑「未快取的外部資源載入失敗」：CDP 開**清空 localStorage/快取的全新 profile** 抓 `Runtime.exceptionThrown`，一抓就中。
- 2026-07-13 起 CSP 已**完全移除** `unpkg`／`googleapis`／`gstatic` 白名單、`sw.js` 快取只留同源 `/static/`；如再引入其他庫，放 `/static/vendor/` 並確認 CSP 不需回加外部域。

### 非同步競態
- `_bgLoadGen`：每次新背景載入前 `++`，所有 async loop 每輪比對 `myGen === _bgLoadGen`，不符即退出。
- `replayActive`：replay 中任何改圖表的操作（含 `_bgScheduleIndicators`／`_bgLoadOlderBars`）必須先檢查此旗標。

### 主圖標記（FVG/SMC 等）重繪要完整
- 標記經 `_has()` 過濾＝**只顯示時間存在於當下 `ohlcvData` 的棒** → 任何「資料變多／換內容」的時點都必須用暫存重繪，否則整段沒標記。已因漏重繪出過兩次 bug：
  1. `_bgLoadOlderBars` 補載歷史完成後（02b429a）；
  2. `_fetchWinRateNow` **快取命中分支**——必須與網路成功路徑重繪**同一組層**（fvg_ms/fvg_break/fvg_trades/fvg_bb/SMC 掃蕩·結構·OB·SR/VWAP/通道/pd_ranges），少一個就是切標的回來沿用舊標的標記（ca8ec0f）。
- 之後在勝率回應新增圖層時，**兩條路徑都要加**。
- 各時框可看深度：背景補載僅 1m/5m/15m/1h/4h；**1d/1w/1M 一次載入** → 舊區段「沒 K 棒也沒標記」是設計、不是 bug。
  ⚠ 2026-08-18 更正：**8h/2h/30m 早已移除**（`config.js` 的 `TF_LABELS` 就是可用時框白名單，那裡寫著「已移除」），
  後端對它們一律回 400。本檔上面幾處提到 30m/2h 的都是**當年的事故紀錄**、不是現況。寫測試前先從 `TF_LABELS` 抽清單，別寫死。

### 極簡模式（perf-mode）不可污染正常模式
- `savePrefs()`（utils.js）在 perf-mode 直接 `return` — 否則 in-memory perf palette 會被寫回 `localStorage.chartColors`。
- `showLegColorPopup()`（draw.js）在 perf-mode 直接 `return`。
- topbar 相關覆寫必須用 `!important`（壓過 style.css 末段「橘子熊可愛風格」的 `!important`）。完整機制見 [docs/frontend.md](docs/frontend.md)。

### Pionex 限流 / 行情資料源
- Pionex API：10 次/秒/IP，超過封鎖 60s 且重試會 +10s 永遠清不掉。**Binance fapi 同理**（418/429 全域熔斷，`_BINANCE_COOLDOWN_UNTIL`）。行情/價格走 Binance，Pionex 僅用於標的清單（硬碟快取 24hr）與獨有標的 klines。
- **crypto perp K 線 fallback 鏈（2026-07-10 定版）**：**Binance fapi → Bybit（`category=linear`）→ Pionex**。⚠ 順序重要：**Pionex 日線偶有損毀殘棒**（如 BTC 2025-08-14 收盤 121583 vs Binance 118242 → 生假 2.86% FVG、錯收盤），Bybit 則貼合 Binance → 故 Bybit 優先、Pionex 墊底（只給 Bybit 沒有的獨有幣）。Bybit v5 無原生 8h/30m → `_fetch_bybit` 由 4h/15m 重採樣（origin=epoch 對齊 UTC）。
- **降級來源防污染**：`fetch_crypto_ohlcv` 每次回傳標記實際來源（`last_fetch_source()`）；`get_crt_winrate` 只有『來源＝Binance』才寫 7 天磁碟長效快取，降級來源（Bybit/Pionex）只放記憶體＋標 `:deg`、Binance 冷卻一結束就丟棄重抓、冷卻中不做尾巴 concat → 避免髒資料被烤進長效快取而持久化（**Railway 亦會撞冷卻，非只本機**）。診斷「策略標記怪」先看 Binance 是否冷卻（`_BINANCE_COOLDOWN_UNTIL`）掉了 fallback。詳見 memory `project_fallback-source-tagging-antipoison`。
- **台股即時個股分鐘 K = cnyes**（`data/cnyes_futures.py` `fetch_cnyes_stock_intraday`，同台指期源、連續無跳號、無延遲、免金鑰）；get_latest / ohlcv 初次載入 / fetch_crt_df 三處當日主源，歷史仍 yfinance，Fugle 退為備援。詳見 [docs/backend.md](docs/backend.md)。

### 不可更改的設定
- 報價輪詢週期：**crypto 1 秒、台股 3 秒**（行情即時性需求），**禁止以「減輕伺服器負擔」為由改慢**。
  2026-08-23 起實際排程是**相位對齊**（`_tkSchedule`／`_tkNextDelay`）：用回應的 `ts` 把下一次排在
  「伺服器剛更新完」之後 120ms，上面那兩個數字是**沒有 ts 時的退路**。
  ⚠ 2026-09-18：對齊改成**量測伺服器產出週期**（原本假設＝自己的輪詢週期）。台股產出 5 秒／輪詢 3 秒，
  舊公式會算出負數被夾成下限 250ms ＝ **每 0.25 秒打一次**（台股當初就是為此被排除；伺服器一停更連加密也會中）。
  守門員 `node scripts/check_ticker_phase.js`。
- 台股疊價 `_tw_rt_overlay_worker` 每 5 秒：**主源 cnyes 批次報價**（一請求 500 檔、每檔 36 bytes、0.67 秒）
  ＋備源 TWSE MIS。每輪打 1000 檔（使用者正在看的＋前 50 高量＋輪掃）→ **約 15 秒**覆蓋全台股。
  ⚠ **不假設 cnyes 即時**：盤中量它自帶的報價時間戳，落後超過 3 分鐘就退回 MIS 十分鐘。
  台股全量清單仍由 `_tw_ticker_worker` 每 30 秒抓 TWSE/TPEX opendata 維護。
  ⚠ **基底每 30 秒整包覆蓋，疊上去的今日價會被洗掉** → `live_data._TW_OV` 記住疊過什麼、每次覆蓋前重貼（2026-09-18，使用者：「收盤了但跟我看的有誤差」）。收盤補齊的判準是**「今天補齊了沒」不是時鐘窗口**（寫死 13:35~14:30 的話，14:52 部署重啟就補不到＝線上抽樣 8/10 檔退回昨天的價）。細節與「估價不可蓋掉官方收盤價」見 [docs/backend.md](docs/backend.md)。

## 圖片資源
所有原始圖片存放於 **桌面 `Claude-分類/虛擬貨幣/`**，已複製至 `frontend/static/img/`。對應表與前端使用位置見 [docs/frontend.md](docs/frontend.md)。
